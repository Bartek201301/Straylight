---
phase: 06-database-migration-cleanup
plan: 02
type: execute
wave: 2
depends_on: ['06-01']
files_modified:
  - supabase/check_migrations_status.sql
  - supabase/schema_verification.sql
  - supabase/test_affiliate_library_table.sql
  - supabase/test_articles_table.sql
  - supabase/test_library_items_table.sql
  - supabase/test_votes_table.sql
autonomous: false
requirements: [DB-02, DB-03]

must_haves:
  truths:
    - 'All root-level SQL test/verification scripts approved for deletion by user'
    - 'Approved SQL files are deleted from supabase/ root'
    - 'npm run build and npm run lint pass after all deletions'
    - 'supabase/ directory contains only README.md, migrations/, functions/, and .temp/'
    - 'Database schema is completely unchanged'
  artifacts: []
  key_links: []
---

<objective>
Present the 6 root-level SQL files in supabase/ for user review, then delete approved files and run final verification.

Purpose: Per locked user constraint, ambiguous SQL files must be flagged for user review before deletion. While research indicates all 6 files are one-time verification/test scripts, the user noted they "might use some SQL files for local dev" -- so explicit confirmation is required.

Output: Clean supabase/ root with only README.md, migrations/, functions/, and .temp/ remaining.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-database-migration-cleanup/06-RESEARCH.md
@.planning/phases/06-database-migration-cleanup/06-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: User review of root-level SQL files</name>
  <decision>Which of the 6 root-level SQL files should be deleted vs kept?</decision>
  <context>
All 6 files are one-time verification/test scripts designed to be run in the Supabase SQL Editor. None are referenced by application code. Research recommends deleting all of them. However, per the locked constraint "Flag ambiguous root SQL files for user review", explicit confirmation is needed.

Here are the files:

1. **check_migrations_status.sql** (3.8KB) -- Checks if core tables (users, articles, library_items, votes) exist. One-time verification script.

2. **schema_verification.sql** (10.1KB) -- Comprehensive schema verification that checks table structure, RLS policies, triggers, and includes test data insertion. One-time verification script.

3. **test_affiliate_library_table.sql** (4.9KB) -- Tests the affiliate_library table structure, RLS policies, and operations. One-time test script.

4. **test_articles_table.sql** (4.2KB) -- Tests the articles table structure and operations. One-time test script.

5. **test_library_items_table.sql** (7.6KB) -- Tests the library_items table structure and operations. One-time test script.

6. **test_votes_table.sql** (9.3KB) -- Tests the votes table structure and operations. One-time test script.

All content is preserved in git history and recoverable at any time.
</context>
<options>

<option id="delete-all">
<name>Delete all 6 files (recommended)</name>
<pros>Cleanest result. All are one-time scripts. Git preserves history. Consistent with Phase 3 cleanup approach.</pros>
<cons>If you actively use any for local dev, you'd need to recover from git.</cons>
</option>
<option id="keep-some">
<name>Keep specific files</name>
<pros>Preserves any files you actively use for local development.</pros>
<cons>Partial cleanup. Specify which files to keep.</cons>
</option>
<option id="keep-all">
<name>Keep all 6 files</name>
<pros>No risk of losing actively used scripts.</pros>
<cons>Root SQL clutter remains.</cons>
</option>
</options>
<action>Present the 6 root-level SQL files to the user for review. Wait for user decision before proceeding.</action>
<verify>User has explicitly selected one of: delete-all, keep-some, or keep-all.</verify>
<done>User decision recorded. Proceed to Task 2 with the approved file list.</done>
<resume-signal>Select: delete-all, keep-some (list which to keep), or keep-all</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Delete approved SQL files and run final verification</name>
  <files>supabase/check_migrations_status.sql, supabase/schema_verification.sql, supabase/test_affiliate_library_table.sql, supabase/test_articles_table.sql, supabase/test_library_items_table.sql, supabase/test_votes_table.sql</files>
  <action>
Based on user decision from Task 1:

**If delete-all:** Delete all 6 root-level SQL files from supabase/.

**If keep-some:** Delete only the files the user approved for deletion. Keep the specified files.

**If keep-all:** Skip deletion. Proceed to verification only.

After deletions (if any), run full verification:

1. `npm run build` -- must pass (confirms no code references deleted files)
2. `npm run lint` -- must pass (no new issues)
3. `ls supabase/` -- verify only expected contents remain (README.md, migrations/, functions/, .temp/, plus any user-kept SQL files)

DO NOT execute any SQL or DDL operations against the production database.
</action>
<verify>
Run: `npm run build` -- must exit 0
Run: `npm run lint` -- must exit 0
Run: `ls supabase/` -- should show only README.md, migrations/, functions/, .temp/ (plus any user-kept files)
</verify>
<done>All user-approved SQL files deleted. Build and lint pass. supabase/ directory is clean per user's decision. Phase 6 complete.</done>
</task>

</tasks>

<verification>
1. User explicitly reviewed and approved the disposition of all 6 root-level SQL files
2. `npm run build` passes after all deletions
3. `npm run lint` passes after all deletions
4. `ls supabase/` shows only: README.md, migrations/, functions/, .temp/ (plus any user-kept files)
5. Zero DDL operations executed against production database
6. All phase requirements (DB-01, DB-02, DB-03) satisfied
</verification>

<success_criteria>

- User reviewed all 6 root SQL files and made explicit deletion decision
- Approved files deleted, build and lint pass
- supabase/ directory matches user's intended state
- Database schema unchanged throughout entire phase
  </success_criteria>

<output>
After completion, create `.planning/phases/06-database-migration-cleanup/06-02-SUMMARY.md`
</output>
