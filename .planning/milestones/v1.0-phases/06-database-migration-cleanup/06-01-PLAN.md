---
phase: 06-database-migration-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/*.sql
  - supabase/migrations/.gitkeep
  - supabase/README.md
autonomous: true
requirements: [DB-01, DB-03]

must_haves:
  truths:
    - 'All 42 local migration files are deleted from supabase/migrations/'
    - 'supabase/migrations/ directory preserved with .gitkeep for future Supabase CLI compatibility'
    - 'supabase/README.md accurately reflects current architecture (dashboard-managed migrations, not local files)'
    - 'npm run build succeeds after file deletions (no code referenced these files)'
    - 'Database schema is completely unchanged (zero DDL operations executed)'
  artifacts:
    - path: 'supabase/migrations/.gitkeep'
      provides: 'Empty directory placeholder for Supabase CLI compatibility'
    - path: 'supabase/README.md'
      provides: 'Updated documentation reflecting current migration architecture'
      contains: 'managed via Supabase'
  key_links:
    - from: 'supabase/README.md'
      to: 'supabase/migrations/'
      via: 'Documentation references empty migrations directory'
      pattern: 'migrations'
---

<objective>
Delete all 42 obsolete local migration files from supabase/migrations/ and update supabase/README.md to reflect the current architecture where migrations are managed via the Supabase dashboard, not local files.

Purpose: The 42 local migration files are completely disconnected from production (zero version overlap with the 48 production migration records). They use two incompatible naming conventions with duplicate numbers (025, 026). Keeping them creates confusion about what drives the production schema.

Output: Clean supabase/migrations/ directory with .gitkeep, updated README.md documenting current workflow.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-database-migration-cleanup/06-RESEARCH.md
@supabase/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete all 42 migration files and preserve directory</name>
  <files>supabase/migrations/*.sql, supabase/migrations/.gitkeep</files>
  <action>
Delete all 42 SQL files from supabase/migrations/:
- 35 sequential files (001_create_users_table.sql through 035_fix_function_search_paths.sql, including duplicate-numbered 025 and 026 pairs)
- 7 timestamp-based files (20241203_performance_functions.sql through 20250906020600_fix_security_definer_views_phase3.sql)

After deletion, create an empty `.gitkeep` file in supabase/migrations/ to preserve the directory for future Supabase CLI compatibility (the CLI expects this directory to exist for `supabase db push`).

These files are safe to delete because:

1. Zero overlap between local file version numbers and production migration records
2. No application code imports or references any .sql file (grep confirmed)
3. Git history preserves all content for recovery if ever needed

DO NOT touch supabase/functions/ or supabase/.temp/ -- those are active and out of scope.
DO NOT execute any SQL or DDL operations against the production database.
</action>
<verify>
Run: `ls supabase/migrations/` -- should show only `.gitkeep`
Run: `ls supabase/migrations/*.sql 2>/dev/null | wc -l` -- should be 0
Run: `ls supabase/functions/` -- should be unchanged (edge functions still present)
</verify>
<done>All 42 SQL migration files deleted. supabase/migrations/ directory contains only .gitkeep. supabase/functions/ and supabase/.temp/ untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite supabase/README.md for current architecture</name>
  <files>supabase/README.md</files>
  <action>
Replace the entire contents of supabase/README.md. The current README (11.5KB) documents the old manual-apply workflow ("copy paste into SQL Editor, click Run") which is completely outdated.

Write a concise README that covers:

1. **Overview**: StrayLight uses Supabase (PostgreSQL) with migrations managed via the Supabase dashboard/CLI, not local files.

2. **Directory Structure**: Explain what each subdirectory contains:
   - `migrations/` -- Reserved for future Supabase CLI migrations (currently empty; historical migration files were removed as they were disconnected from production tracking)
   - `functions/` -- Supabase Edge Functions (actively deployed)
   - `.temp/` -- CLI temporary files

3. **Migration History**: Brief note that the production database tracks 48 migrations applied via the Supabase dashboard. The schema evolution history was previously maintained as local SQL files but these became disconnected from production tracking (different version numbers, naming conventions). The original files are preserved in git history for reference.

4. **Current Schema Summary**: Keep the existing table schema summaries (Users, Articles, Library Items, Votes, Affiliate Library, Notifications, Newsletter Subscriptions) but present them as a compact reference, not step-by-step instructions.

5. **Making Schema Changes**: Note that schema changes should be made via the Supabase dashboard SQL Editor or Supabase CLI (`supabase db push`). Reference the RLS policies as a key security feature.

Keep the README under 150 lines. Do not include the old step-by-step "copy paste into SQL Editor" instructions. Do not include setup instructions for tables that already exist in production.

Preserve the schema SQL blocks for Users, Articles, Library Items, and Votes tables from the existing README as a reference section -- these are useful documentation of the current schema shape.
</action>
<verify>
Run: `wc -l supabase/README.md` -- should be under 150 lines
Confirm the README no longer contains "copy and paste" or "click Run" instructions.
Confirm the README mentions dashboard-managed migrations and references edge functions.
</verify>
<done>supabase/README.md is a concise, accurate reference for the current Supabase architecture. No outdated manual-apply instructions remain.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes -- confirms no application code referenced deleted migration files
2. `npm run lint` passes -- no new lint issues introduced
3. `ls supabase/migrations/` shows only `.gitkeep` (42 files removed)
4. `ls supabase/functions/` unchanged (edge functions untouched)
5. supabase/README.md is under 150 lines and references current architecture
6. Zero DDL operations executed against production database
</verification>

<success_criteria>

- All 42 migration files deleted from supabase/migrations/
- .gitkeep preserves the directory for future CLI use
- supabase/README.md reflects dashboard-managed migration architecture
- Build and lint pass cleanly
- No schema changes to production database
  </success_criteria>

<output>
After completion, create `.planning/phases/06-database-migration-cleanup/06-01-SUMMARY.md`
</output>
