---
phase: 02-dead-code-elimination
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/Footer.tsx
  - src/components/layout/Grid.tsx
  - src/components/layout/Hero.tsx
  - src/components/layout/PageHeader.tsx
  - src/components/articles/ArticlesPageContent.tsx
autonomous: true
requirements: [DEAD-02, DEAD-05]
gap_closure: true

must_haves:
  truths:
    - 'src/components/layout/ contains only files with at least one import consumer in src/'
    - 'ArticlesPageContent.tsx contains no {false && ...} dead JSX blocks'
    - 'Build passes and lint is clean after all deletions'
  artifacts:
    - path: 'src/components/articles/ArticlesPageContent.tsx'
      provides: 'Articles page content without dead JSX scaffolding'
      contains: 'false &&'
      must_not_contain: true
  key_links:
    - from: 'src/components/layout/'
      to: 'grep across src/'
      via: 'zero import matches for Footer, Grid, Hero, PageHeader'
      pattern: 'layout/Footer|layout/Grid|layout/Hero|layout/PageHeader'
---

<objective>
Close the two remaining gaps from Phase 2 verification: delete 4 unused layout component files and remove 2 dead `{false && ...}` JSX blocks from ArticlesPageContent.tsx.

Purpose: Fully satisfy DEAD-02 (unused file removal) and DEAD-05 (obsolete dead code removal) so Phase 2 verification passes at 6/6 and Phase 3 can begin.
Output: 4 files deleted, 1 file modified (8 lines removed), build and lint passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dead-code-elimination/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete 4 unused layout components</name>
  <files>
    src/components/layout/Footer.tsx
    src/components/layout/Grid.tsx
    src/components/layout/Hero.tsx
    src/components/layout/PageHeader.tsx
  </files>
  <action>
    Delete all four files. Grep has confirmed zero import consumers across all of src/ for each file (pattern `layout/Footer|layout/Grid|layout/Hero|layout/PageHeader` returns no matches).

    Delete in this order to make the batch reviewable:
    1. src/components/layout/Footer.tsx
    2. src/components/layout/Grid.tsx
    3. src/components/layout/Hero.tsx
    4. src/components/layout/PageHeader.tsx

    Do NOT delete any other files in src/components/layout/ — the following are active and must remain:
    - AuthenticatedFooter.tsx (used by ConditionalFooter.tsx)
    - ConditionalFooter.tsx (used in app layout)
    - Container.tsx (active utility)
    - Navigation.tsx (active)
    - PublicFooter.tsx (active)

    After deletion, verify the layout directory still contains those 5 active files and the 4 targets are gone.

    Run `npm run build` and `npm run lint` after deletion to confirm no regressions.

  </action>
  <verify>
    - `git status` shows 4 deleted files
    - Grep for `layout/Footer|layout/Grid|layout/Hero|layout/PageHeader` across src/ returns zero matches
    - `npm run build` exits 0
    - `npm run lint` exits 0
  </verify>
  <done>
    Footer.tsx, Grid.tsx, Hero.tsx, and PageHeader.tsx are absent from the filesystem. Build and lint pass with zero errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove dead {false && ...} JSX blocks from ArticlesPageContent.tsx</name>
  <files>
    src/components/articles/ArticlesPageContent.tsx
  </files>
  <action>
    Remove two dead JSX blocks from src/components/articles/ArticlesPageContent.tsx.

    Block 1 (in the loading branch, ~lines 143-146):
    ```
    {false && (
      <></>
      // <Aurora colorStops={["#FFFFFF", "#F8F8FF", "#F0F0F0"]} blend={0.5} amplitude={1.0} speed={0.5} />
    )}
    ```

    Block 2 (in the return branch, ~lines 182-185):
    ```
    {false && (
      <></>
      // <Aurora colorStops={["#FFFFFF", "#F8F8FF", "#F0F0F0"]} blend={0.5} amplitude={1.0} speed={0.5} />
    )}
    ```

    Delete each block entirely (all 4 lines per block, including the surrounding `{false && (` and `)}` delimiters). Do not alter any surrounding code — the `<div className=...>` opening tags and `<main ...>` tags immediately adjacent must remain untouched.

    The Aurora component was already deleted in Plan 02-01. These blocks are guaranteed never to render (`false &&` short-circuits always). Removing them satisfies DEAD-05.

    Do NOT remove the `false ?` ternary on line ~223 (`false ? 'text-black/70' : 'text-white/70'`) — that is a separate active conditional expression choosing a CSS class, not a dead render block.

    After editing, run `npm run build` and `npm run lint` to confirm zero errors.

  </action>
  <verify>
    - Grep for `false &&` in ArticlesPageContent.tsx returns zero matches (or only the line-223 ternary if it uses that pattern — confirm it is not a JSX block)
    - `npm run build` exits 0
    - `npm run lint` exits 0
    - File still renders the loading skeleton and the articles grid correctly (structure intact)
  </verify>
  <done>
    ArticlesPageContent.tsx contains no {false && ...} JSX render blocks. The file compiles without errors. Build and lint pass.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `git diff --stat HEAD~2` shows 4 deletions and 1 modification
- Grep for `layout/Footer|layout/Grid|layout/Hero|layout/PageHeader` across src/ returns zero results
- Grep for `false &&` in ArticlesPageContent.tsx returns zero JSX block matches
- `npm run build` exits 0
- `npm run lint` exits 0
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>

1. Footer.tsx, Grid.tsx, Hero.tsx, PageHeader.tsx are deleted — zero consumers existed, zero regressions
2. ArticlesPageContent.tsx has zero {false && ...} JSX dead render blocks
3. Build, lint, and TypeScript compilation all pass
4. DEAD-02 and DEAD-05 requirements are fully satisfied — Phase 2 verification can re-run at 6/6
   </success_criteria>

<output>
After completion, create `.planning/phases/02-dead-code-elimination/02-05-SUMMARY.md` following the summary template.
</output>
