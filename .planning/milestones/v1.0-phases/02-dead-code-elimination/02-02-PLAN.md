---
phase: 02-dead-code-elimination
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/components/articles/ArticleStatusNotification.tsx
  - src/components/articles/ArticlesList.tsx
  - src/components/articles/ReadingProgress.tsx
  - src/components/articles/ReadingTimeDisplay.tsx
  - src/components/auth/AccessRequired.tsx
  - src/components/auth/AuthGate.tsx
  - src/components/newsletter/HomeNewsletterSignup.tsx
  - src/components/newsletter/NewsletterForm/
  - src/components/newsletter/NewsletterSignup.tsx
  - src/components/NewsletterForm.tsx
  - src/components/admin/NotificationManagement.tsx
  - src/components/affiliate/AffiliateLink.tsx
  - src/components/notifications/NotificationBell.tsx
  - src/components/search/
  - src/components/forms/ValidatedInput.tsx
  - src/components/forms/ValidatedTextarea.tsx
  - src/hooks/
  - src/lib/
autonomous: true
requirements:
  - DEAD-02
must_haves:
  truths:
    - 'All genuinely unused medium-risk component files are deleted (articles, auth, newsletter, admin, affiliate, notifications, search, forms)'
    - 'All genuinely unused hooks are deleted (~12 files in src/hooks/)'
    - 'All genuinely unused lib files are deleted (~15 files in src/lib/)'
    - 'Build passes, lint passes, and TypeScript compiles after all deletions'
  artifacts: []
  key_links: []
---

<objective>
Remove unused medium-risk component files, hooks, and lib utility files in category batches with verification between each batch.

Purpose: Complete the file-level dead code elimination by removing the remaining ~50 unused files in higher-coupling categories (articles, auth, newsletter, hooks, lib utilities).
Output: ~50 fewer unused files, verified clean build after each category batch.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dead-code-elimination/02-RESEARCH.md
@.planning/phases/02-dead-code-elimination/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete unused component files (articles, auth, newsletter, admin, search, forms)</name>
  <files>
    src/components/articles/ArticleStatusNotification.tsx
    src/components/articles/ArticlesList.tsx
    src/components/articles/ReadingProgress.tsx
    src/components/articles/ReadingTimeDisplay.tsx
    src/components/auth/AccessRequired.tsx
    src/components/auth/AuthGate.tsx
    src/components/newsletter/HomeNewsletterSignup.tsx
    src/components/newsletter/NewsletterForm/
    src/components/newsletter/NewsletterSignup.tsx
    src/components/NewsletterForm.tsx
    src/components/admin/NotificationManagement.tsx
    src/components/affiliate/AffiliateLink.tsx
    src/components/notifications/NotificationBell.tsx
    src/components/search/*
    src/components/forms/ValidatedInput.tsx
    src/components/forms/ValidatedTextarea.tsx
  </files>
  <action>
For EACH file, follow the verify-then-delete workflow per user's locked decision:

1. **Grep for the component name** across src/ and config files
2. **Only delete if zero consumers found** (self-references and commented-out imports don't count)
3. **Delete in category batches**, running `npm run build && npm run lint && npx tsc --noEmit` after each batch

**Batch F — src/components/articles/ (4 files):**
ArticleStatusNotification.tsx, ArticlesList.tsx, ReadingProgress.tsx, ReadingTimeDisplay.tsx

- Be careful: some article components ARE used. Only delete the 4 Knip-flagged ones.
- After batch: build+lint+tsc check

**Batch G — src/components/auth/ (2 files):**
AccessRequired.tsx, AuthGate.tsx

- NOTE: Do NOT delete ProtectedRoute.tsx (actively used). Only the 2 listed files.
- After batch: build+lint+tsc check

**Batch H — src/components/newsletter/ + root NewsletterForm (4 files):**
HomeNewsletterSignup.tsx, NewsletterForm/ (directory), NewsletterSignup.tsx, src/components/NewsletterForm.tsx (root-level)

- Verify which newsletter components are actually imported in pages before deleting.
- After batch: build+lint+tsc check

**Batch I — src/components/ remaining (admin, affiliate, notifications, search, forms) (~6 files):**
admin/NotificationManagement.tsx, affiliate/AffiliateLink.tsx, notifications/NotificationBell.tsx, search/\* (SearchInput, SearchResults, etc.), forms/ValidatedInput.tsx, forms/ValidatedTextarea.tsx

- After batch: build+lint+tsc check

**IMPORTANT:** If grep reveals a file IS imported somewhere, do NOT delete it. Log it as a false positive.

**IMPORTANT:** If a batch breaks the build, isolate the causing file, restore it, and continue. Log as false positive.

**IMPORTANT:** Clean up empty parent directories after deletions.
</action>
<verify>After all batches: `npm run build && npm run lint && npx tsc --noEmit` all pass.</verify>
<done>All genuinely unused medium-risk component files deleted. Each batch verified with build+lint+tsc.</done>
</task>

<task type="auto">
  <name>Task 2: Delete unused hooks and lib utility files</name>
  <files>
    src/hooks/useAdvancedIntersectionObserver.ts
    src/hooks/useArticleValidation.ts
    src/hooks/useErrorRecovery.ts
    src/hooks/useIntersectionObserver.ts
    src/hooks/useNotificationErrors.ts
    src/hooks/useNotificationPerformance.ts
    src/hooks/useRetry.ts
    src/hooks/useScrollSync.ts
    src/hooks/useSlugManagement.ts
    src/hooks/useSubmissionFlow.ts
    src/hooks/useSubmissionWithRecovery.ts
    src/hooks/useTagManagement.ts
    src/lib/auth/index.ts
    src/lib/database/
    src/lib/errors/submission-errors.ts
    src/lib/hooks/useSearchPerformance.ts
    src/lib/imageLoader.js
    src/lib/monitoring.ts
    src/lib/responsive-utils.ts
    src/lib/revalidation.ts
    src/lib/services/notification-service.ts
    src/lib/theme-utils.ts
    src/lib/utils/imageUtils.ts
    src/lib/utils/retry.ts
    src/lib/validation/article-submission.ts
  </files>
  <action>
For EACH file, follow the verify-then-delete workflow:

1. **Grep for the hook/function name** (not just filename) across src/ and config files
2. **Only delete if zero consumers found**
3. **Delete in category batches** with build checks

**Batch J — src/hooks/ (~12 files):**
useAdvancedIntersectionObserver, useArticleValidation, useErrorRecovery, useIntersectionObserver, useNotificationErrors, useNotificationPerformance, useRetry, useScrollSync, useSlugManagement, useSubmissionFlow, useSubmissionWithRecovery, useTagManagement

- These are highest-coupling risk -- grep thoroughly for each hook name.
- After batch: build+lint+tsc check

**Batch K — src/lib/ (~15 files):**
auth/index.ts, database/\* (2 files), errors/submission-errors.ts, hooks/useSearchPerformance.ts, imageLoader.js, monitoring.ts, responsive-utils.ts, revalidation.ts, services/notification-service.ts, theme-utils.ts, utils/imageUtils.ts, utils/retry.ts, validation/article-submission.ts

**SPECIAL ATTENTION for src/lib/ files:**

- `src/lib/auth/index.ts` -- this is a barrel file. Check if anything imports from `@/lib/auth` (barrel) vs `@/lib/auth/specific-file` (direct). If barrel consumers exist, do NOT delete.
- `src/lib/imageLoader.js` -- check next.config.js for `loader` configuration referencing this file.
- `src/lib/monitoring.ts` -- check if referenced in any middleware or API routes.
- `src/lib/revalidation.ts` -- check API routes for revalidation usage.

- After batch: build+lint+tsc check

**IMPORTANT:** The src/lib/ batch has the highest coupling risk. If more than 2 files cause build failures, consider splitting into smaller sub-batches.

**IMPORTANT:** Clean up empty parent directories after deletions.
</action>
<verify>After all batches: `npm run build && npm run lint && npx tsc --noEmit` all pass.</verify>
<done>All genuinely unused hooks and lib files deleted. Build, lint, and TypeScript compilation all pass. False positives documented.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `npm run lint` passes
3. `npx tsc --noEmit` passes with zero errors
4. Approximately 40-50 additional files removed beyond Plan 02-01
5. No actively-used files were deleted (any false positives restored and documented)
</verification>

<success_criteria>

- All genuinely unused component, hook, and lib files deleted
- Build, lint, and TypeScript compilation pass after each batch and at end
- False positives documented in summary
- Empty directories cleaned up
  </success_criteria>

<output>
After completion, create `.planning/phases/02-dead-code-elimination/02-02-SUMMARY.md`
</output>
