---
phase: 02-dead-code-elimination
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - package.json
  - package-lock.json
  - src/app/about/page.tsx
  - src/app/write/page.tsx
  - src/app/admin/articles/PendingArticlesList.tsx
  - src/app/articles/ArticlesPageContent.tsx
autonomous: true
requirements:
  - DEAD-04
  - DEAD-05
must_haves:
  truths:
    - 'All confirmed unused npm dependencies removed from package.json'
    - 'DevDependencies removed first, then runtime, with build check between'
    - 'npm audit run and safe fixes applied'
    - 'Commented-out import lines removed from active files'
    - 'TODO/FIXME comments left in place (6 total, all relevant)'
    - 'Build passes, lint passes, and TypeScript compiles after all changes'
  artifacts:
    - path: 'package.json'
      provides: 'Cleaned dependency list with unused packages removed'
  key_links: []
---

<objective>
Remove unused npm dependencies (devDependencies first, then runtime) and clean up commented-out code blocks.

Purpose: Reduce package footprint and eliminate dead commented-out code. This is the final cleanup step of Phase 2, completing the dead code elimination before moving to scratchpad cleanup.
Output: 6 unused packages removed from package.json, commented-out imports cleaned, npm audit run.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dead-code-elimination/02-RESEARCH.md
@.planning/phases/02-dead-code-elimination/02-03-SUMMARY.md
@package.json
@next.config.js
@tailwind.config.js
@postcss.config.js
@.eslintrc.json
@tsconfig.json
@.lintstagedrc.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused npm dependencies</name>
  <files>package.json, package-lock.json</files>
  <action>
Follow the user's locked dependency removal strategy: devDependencies first, then runtime, with config cross-checks.

**Step 1 — Cross-check ALL config files before any removal:**
For each dependency below, grep these config files to confirm zero references:

- next.config.js
- tailwind.config.js
- postcss.config.js
- .eslintrc.json
- tsconfig.json
- .lintstagedrc.json
- middleware.ts
- components.json

**Step 2 — Remove devDependencies (3 packages):**

```bash
npm uninstall @tailwindcss/aspect-ratio @tailwindcss/forms is-ci
```

- `@tailwindcss/aspect-ratio` — confirmed NOT in tailwind.config.js plugins array
- `@tailwindcss/forms` — confirmed NOT in tailwind.config.js plugins array
- `is-ci` — confirmed zero references anywhere

After removal: `npm run build && npm run lint && npx tsc --noEmit`

**Step 3 — Remove runtime dependencies (3 packages):**

```bash
npm uninstall class-variance-authority motion remark
```

- `class-variance-authority` — zero imports in src/ (cva is not used)
- `motion` — zero imports (project uses `framer-motion` package, not `motion`)
- `remark` — zero imports (project uses remark-\* sub-packages like remark-gfm, remark-html, not the base `remark` package)

IMPORTANT: Before removing `remark`, verify that removing the base package does not uninstall remark-\* sub-packages. Run `npm ls remark` to check the dependency tree. If remark-gfm or remark-html depend on `remark` as a peer dependency, do NOT remove it.

After removal: `npm run build && npm run lint && npx tsc --noEmit`

**Step 4 — Run npm audit:**

```bash
npm audit
```

- Review output for vulnerabilities
- Run `npm audit fix` to apply safe fixes (non-breaking, no major version bumps)
- If `npm audit fix` proposes breaking changes, document them in the summary for future action but do NOT apply

**Step 5 — Clean node_modules:**

```bash
npm prune
```

  </action>
  <verify>
`npm run build && npm run lint && npx tsc --noEmit` all pass.
`npm ls class-variance-authority motion remark @tailwindcss/aspect-ratio @tailwindcss/forms is-ci` shows none found (all removed).
`npm audit` output reviewed.
  </verify>
  <done>6 unused dependencies removed (3 dev, 3 runtime). Build, lint, TypeScript pass. npm audit run and safe fixes applied. Any remaining audit findings documented.</done>
</task>

<task type="auto">
  <name>Task 2: Remove commented-out code blocks</name>
  <files>
    src/app/about/page.tsx
    src/app/write/page.tsx
    src/app/admin/articles/PendingArticlesList.tsx
    src/app/articles/ArticlesPageContent.tsx
  </files>
  <action>
Per user's locked decision: review each commented block individually, no blanket removal.

**Commented-out imports to remove (6 confirmed from research):**

1. `src/app/about/page.tsx` lines ~10-11 — commented-out imports of `Link` and `Aurora`
   - Remove these commented-out import lines

2. `src/app/write/page.tsx` lines ~18, ~25 — commented-out imports of `MobileEditorToolbar` and `ArticleStatusNotification`
   - Both reference files that were deleted in Plans 02-01/02-02
   - Remove these commented-out import lines

3. `src/app/admin/articles/PendingArticlesList.tsx` line ~4 — commented-out import of `getSupabaseAdmin`
   - Remove the commented-out import line (an explanatory comment above it may exist -- keep the explanatory comment if it explains WHY the import was removed, delete if it's just the commented-out code)

4. `src/app/articles/ArticlesPageContent.tsx` line ~4 — commented-out import of `Aurora`
   - Aurora is a deleted dead code file
   - Remove the commented-out import line

**TODO/FIXME comments (6 total) — LEAVE ALL IN PLACE:**
Per user discretion and research recommendation, all 6 TODO/FIXME comments are relevant documentation of planned features or known gaps. Do not remove them.

**General commented-out code scan:**
After removing the 6 known imports, do a quick scan for any other substantial commented-out code blocks (>3 lines of commented-out functional code, not explanatory comments). Use:

```bash
grep -rn "^[[:space:]]*//" src/ --include="*.ts" --include="*.tsx" | head -50
```

Review any large blocks found. Apply judgment per user's discretion: remove clearly dead commented-out code (old implementations, abandoned features), keep explanatory comments and documentation comments.

After all removals: `npm run build && npm run lint && npx tsc --noEmit`
</action>
<verify>`npm run build && npm run lint && npx tsc --noEmit` all pass. The 6 known commented-out imports are removed. All 6 TODO/FIXME comments still present.</verify>
<done>Commented-out import lines removed from 4 files. TODO/FIXME comments left in place. Build, lint, TypeScript pass. Phase 2 dead code elimination complete.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `npm run lint` passes
3. `npx tsc --noEmit` passes with zero errors
4. package.json no longer contains: class-variance-authority, motion, remark, @tailwindcss/aspect-ratio, @tailwindcss/forms, is-ci
5. npm audit output reviewed and safe fixes applied
6. Commented-out imports removed from 4 files
7. All 6 TODO/FIXME comments still present in codebase
</verification>

<success_criteria>

- 6 unused npm dependencies removed from package.json
- npm audit run, safe fixes applied, remaining findings documented
- 6 commented-out imports removed
- 6 TODO/FIXME comments preserved
- Build, lint, and TypeScript compilation pass
- Phase 2 dead code elimination fully complete
  </success_criteria>

<output>
After completion, create `.planning/phases/02-dead-code-elimination/02-04-SUMMARY.md`
</output>
