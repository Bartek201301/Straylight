---
phase: 02-dead-code-elimination
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - knip.json
  - src/components/effects/Aurora.tsx
  - src/components/effects/BlurText.tsx
  - src/components/effects/GlareHover.tsx
  - src/components/effects/HomeAurora.tsx
  - src/components/effects/RotatingText.tsx
  - src/components/effects/TextPressure.tsx
  - src/components/effects/ViewportAwareCobeGlobe.tsx
  - src/components/effects/ViewportAwareOrb.tsx
  - src/components/error-handling/
  - src/components/editor/EnhancedArticleEditor.tsx
  - src/components/editor/ImageUpload.tsx
  - src/components/editor/MobileEditorToolbar.tsx
  - src/components/editor/RightSidebarToolbar.tsx
  - src/components/editor/SidebarToolbar.tsx
  - src/components/ui/LazyImage.tsx
  - src/components/ui/OptimizedVideo.tsx
  - src/components/ui/background-ripple-effect.tsx
  - src/components/ui/card-hover-effect.tsx
  - src/components/ui/buttons/RetryButton.tsx
  - src/components/ui/display/CodeBlock.tsx
  - src/components/ui/display/FontToggle.tsx
  - src/components/ui/skeletons/
  - src/components/ui/feedback/Toast.tsx
  - src/components/quiz/QuizProgressBar.tsx
  - src/components/quiz/index.ts
autonomous: true
requirements:
  - DEAD-01
  - DEAD-02
must_haves:
  truths:
    - 'Knip config excludes known false positives (.claude/**, supabase/functions/**, test-*.js, scripts/**)'
    - 'All genuinely unused low-risk component files are deleted (effects, error-handling, editor, UI, skeletons, quiz)'
    - 'Build passes, lint passes, and TypeScript compiles after all deletions'
    - 'No false positive files were deleted (public/sw.js, .claude/*, supabase/functions/*)'
  artifacts:
    - path: 'knip.json'
      provides: 'Updated Knip config with ignoreFiles for false positives'
      contains: 'ignoreFiles'
  key_links:
    - from: 'knip.json'
      to: 'npx knip'
      via: 'Knip reads config on invocation'
      pattern: 'ignoreFiles'
---

<objective>
Update Knip configuration to exclude false positives, then remove unused low-risk component files in batches (effects, error-handling, editor, UI primitives, skeletons, quiz).

Purpose: Eliminate the first ~50 genuinely unused files from the lowest-risk categories, reducing the codebase surface area before tackling higher-coupling files.
Output: Updated knip.json, ~50 fewer unused component files, verified clean build.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dead-code-elimination/02-RESEARCH.md
@knip.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Knip config and re-run analysis</name>
  <files>knip.json</files>
  <action>
Update knip.json to add `ignoreFiles` array excluding known false positives:
- `.claude/**`
- `supabase/functions/**`
- `test-*.js`
- `scripts/**`

After updating, run `npx knip --reporter compact` to get the refreshed list of genuinely unused files. Save the output for reference during file deletion tasks.

The updated knip.json should look like:

```json
{
  "$schema": "https://unpkg.com/knip@5/schema.json",
  "ignore": [
    "src/app/**/loading.tsx",
    "src/app/**/error.tsx",
    "src/app/**/not-found.tsx"
  ],
  "ignoreFiles": [
    ".claude/**",
    "supabase/functions/**",
    "test-*.js",
    "scripts/**"
  ],
  "ignoreDependencies": ["@types/*"]
}
```

Verify the updated Knip output no longer flags `.claude/`, `supabase/functions/`, `test-*.js`, or `scripts/**` files.
</action>
<verify>Run `npx knip --reporter compact` and confirm .claude/_, supabase/functions/_, test-_.js, scripts/_ are no longer in the output. Confirm public/sw.js handling (it may still appear -- that is expected, do NOT delete it).</verify>
<done>knip.json updated with ignoreFiles. Knip output no longer contains false positives from excluded directories. Refreshed unused file list is available for subsequent tasks.</done>
</task>

<task type="auto">
  <name>Task 2: Delete unused low-risk component files in category batches</name>
  <files>
    src/components/effects/*
    src/components/error-handling/*
    src/components/editor/EnhancedArticleEditor.tsx
    src/components/editor/ImageUpload.tsx
    src/components/editor/MobileEditorToolbar.tsx
    src/components/editor/RightSidebarToolbar.tsx
    src/components/editor/SidebarToolbar.tsx
    src/components/ui/LazyImage.tsx
    src/components/ui/OptimizedVideo.tsx
    src/components/ui/background-ripple-effect.tsx
    src/components/ui/card-hover-effect.tsx
    src/components/ui/buttons/RetryButton.tsx
    src/components/ui/display/CodeBlock.tsx
    src/components/ui/display/FontToggle.tsx
    src/components/ui/skeletons/*
    src/components/ui/feedback/Toast.tsx
    src/components/quiz/QuizProgressBar.tsx
    src/components/quiz/index.ts
  </files>
  <action>
For EACH file listed below, follow the verify-then-delete workflow:

1. **Grep for the component/file name** across the codebase (src/, config files, middleware.ts) to confirm no imports exist
2. **Only delete if grep finds zero consumers** (the file itself and commented-out imports don't count)
3. **Delete in category batches**, running `npm run build && npm run lint && npx tsc --noEmit` after each batch

**Batch order:**

**Batch A — src/components/effects/ (~8 files):**
Aurora.tsx, BlurText.tsx, GlareHover.tsx, HomeAurora.tsx, RotatingText.tsx, TextPressure.tsx, ViewportAwareCobeGlobe.tsx, ViewportAwareOrb.tsx

- After deleting batch: run build+lint+tsc check

**Batch B — src/components/error-handling/ (~12 files):**
All files in boundaries/, displays/, pages/, validation/ subdirectories that Knip flagged.

- Cross-check: some error-handling files may be used by error.tsx convention files. Grep carefully before deleting.
- After deleting batch: run build+lint+tsc check

**Batch C — src/components/editor/ (5 files):**
EnhancedArticleEditor.tsx, ImageUpload.tsx, MobileEditorToolbar.tsx, RightSidebarToolbar.tsx, SidebarToolbar.tsx

- NOTE: Do NOT delete ArticleEditor.tsx (the main editor) -- only the files listed above.
- After deleting batch: run build+lint+tsc check

**Batch D — src/components/ui/ + skeletons + feedback (~14 files):**
LazyImage.tsx, OptimizedVideo.tsx, background-ripple-effect.tsx, card-hover-effect.tsx, buttons/RetryButton.tsx, display/CodeBlock.tsx, display/FontToggle.tsx, skeletons/ (ArticleSkeleton, CardSkeleton, EditorSkeleton, FormSkeleton, LibraryItemSkeleton, ProfileSkeleton), feedback/Toast.tsx

- After deleting batch: run build+lint+tsc check

**Batch E — src/components/quiz/ (2 files):**
QuizProgressBar.tsx, index.ts

- After deleting batch: run build+lint+tsc check

**IMPORTANT:** If a grep reveals a file IS imported somewhere (not just by itself), do NOT delete it. Log it as a false positive in the summary.

**IMPORTANT:** If a batch deletion breaks the build, identify which specific file caused the breakage. Restore that file (git checkout -- path/to/file), re-run the build, and log it as a false positive. Continue with the remaining deletions.

**IMPORTANT:** After deleting files, if parent directories become empty, remove those empty directories too.
</action>
<verify>
After all batches: `npm run build && npm run lint && npx tsc --noEmit` all pass.
Count deleted files and confirm approximately 40-50 files removed.
</verify>
<done>All genuinely unused low-risk component files deleted across 5 category batches. Each batch verified with build+lint+tsc. No false positives deleted. Empty directories cleaned up.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `npm run lint` passes
3. `npx tsc --noEmit` passes with zero errors
4. knip.json contains ignoreFiles for .claude/**, supabase/functions/**, test-*.js, scripts/**
5. public/sw.js still exists (false positive, not deleted)
6. No files from .claude/ or supabase/functions/ were deleted
</verification>

<success_criteria>

- knip.json updated to exclude false positives
- ~40-50 unused low-risk component files deleted
- Build, lint, and TypeScript compilation all pass after deletions
- Any files that turned out to be used are documented as false positives
  </success_criteria>

<output>
After completion, create `.planning/phases/02-dead-code-elimination/02-01-SUMMARY.md`
</output>
