---
phase: 03-scratchpad-file-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - '03-01'
files_modified:
  - 'NOTIFICATION_SYSTEM NIE USUWAC.md'
  - docs/AFFILIATE_CONFIGURATION.md
  - docs/RLS_SECURITY_DOCUMENTATION.md
autonomous: false
requirements:
  - CLEAN-01
  - CLEAN-02

must_haves:
  truths:
    - 'User has explicitly decided the fate of each borderline file'
    - 'Whitelisted files are confirmed preserved'
    - 'Repository contains only legitimate project files with no scratchpad clutter'
  artifacts: []
  key_links: []
---

<objective>
Present 3 borderline files to the user for explicit keep/delete decision, then execute accordingly. These files were flagged during research because they have legitimate-sounding reasons to keep but also match scratchpad criteria.

Purpose: Honor user's locked decision to "flag borderline files for user review -- don't delete without confirmation" while completing the CLEAN-01 and CLEAN-02 requirements.
Output: All borderline files resolved (kept or deleted per user decision).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scratchpad-file-cleanup/03-RESEARCH.md
@.planning/phases/03-scratchpad-file-cleanup/03-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: User decides on 3 borderline files (NOTIFICATION_SYSTEM, docs/AFFILIATE, docs/RLS)</name>
  <decision>Keep or delete 3 borderline files that were flagged during research</decision>
  <context>
These files match scratchpad criteria (not referenced by code, contain documentation) but have legitimate reasons to keep. Research assessed each one individually.

**File 1: "NOTIFICATION_SYSTEM NIE USUWAC.md"**

- Content: Comprehensive notification system implementation guide
- Keep signal: Filename literally says "DO NOT DELETE" in Polish ("NIE USUWAC")
- Delete signal: Content duplicates what is already implemented in the codebase
- Research recommendation: Keep (strong user intent signal in filename)

**File 2: docs/AFFILIATE_CONFIGURATION.md**

- Content: Affiliate link setup guide with platform details (Amazon Associates, etc.)
- Keep signal: Contains operational configuration info useful for maintainers
- Delete signal: Not referenced by code, could be reconstructed from platform docs
- Research recommendation: Keep (useful operational reference)

**File 3: docs/RLS_SECURITY_DOCUMENTATION.md**

- Content: Comprehensive RLS policy documentation for Supabase
- Keep signal: Serves as security audit reference
- Delete signal: Not referenced by code, RLS policies are documented in migration files
- Research recommendation: Keep (useful security reference)

Note: If all docs/ files are deleted, the docs/ directory itself will also be removed. If any docs/ file is kept, the directory stays.
</context>
<options>

<option id="keep-all">
<name>Keep all 3 files</name>
<pros>Preserves user intent (NIE USUWAC filename), retains operational and security references</pros>
<cons>3 documentation files remain that are not code-referenced</cons>
</option>
<option id="delete-all">
<name>Delete all 3 files</name>
<pros>Maximum cleanup, git history preserves everything</pros>
<cons>Loses "DO NOT DELETE" signal, removes operational references</cons>
</option>
<option id="keep-notification-delete-docs">
<name>Keep NOTIFICATION_SYSTEM, delete both docs/ files</name>
<pros>Honors "DO NOT DELETE" filename, removes less-critical docs</pros>
<cons>Loses affiliate and RLS references</cons>
</option>
<option id="custom">
<name>Custom selection</name>
<pros>Full control</pros>
<cons>None</cons>
</option>
</options>
<resume-signal>Select: keep-all, delete-all, keep-notification-delete-docs, or specify custom (e.g., "keep File 1 and File 3, delete File 2")</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Execute borderline file decisions and final verification</name>
  <files>
    "NOTIFICATION_SYSTEM NIE USUWAC.md"
    docs/AFFILIATE_CONFIGURATION.md
    docs/RLS_SECURITY_DOCUMENTATION.md
  </files>
  <action>
Based on the user's decision from Task 1:

**For each file the user chose to DELETE:**

```bash
rm "filename"
```

For the NOTIFICATION_SYSTEM file specifically (has spaces and Polish characters):

```bash
rm "NOTIFICATION_SYSTEM NIE USUWAC.md"
```

If both docs/ files are deleted, also remove the empty directory:

```bash
rmdir docs/
```

**For each file the user chose to KEEP:**
No action needed -- leave the file in place.

**After executing decisions, run final verification:**

```bash
npm run build
npm run lint
```

**Final root directory audit:**
List all remaining root-level files and confirm every one is either:

1. A whitelisted tool config
2. A core project doc (CLAUDE.md, AGENTS.md, ARCHITECTURE.md, Project.md)
3. A user-approved borderline file
4. Part of the application (src/, public/, supabase/, node_modules/, etc.)
   </action>
   <verify>

- Deleted files are gone: `ls "filename"` fails for each deleted file
- Kept files are present: `ls "filename"` succeeds for each kept file
- `npm run build` exits with code 0
- `npm run lint` exits with code 0
- Root directory listing contains only legitimate project files
  </verify>
  <done>
  All borderline files resolved per user decision. Build and lint pass. Repository root is clean -- contains only legitimate project files, whitelisted configs, and user-approved documentation.
  </done>
  </task>

</tasks>

<verification>
1. User explicitly decided on each of the 3 borderline files
2. Decisions were executed correctly (files deleted or kept as instructed)
3. Whitelisted files confirmed present (chatbot.json, components.json, CLAUDE.md, etc.)
4. `npm run build` passes
5. Root directory contains zero scratchpad clutter
</verification>

<success_criteria>

- All 3 borderline files have explicit user-approved disposition
- Build passes after any deletions
- Repository root is clean with only legitimate files remaining
- Phase 3 requirements CLEAN-01 through CLEAN-06 are fully satisfied
  </success_criteria>

<output>
After completion, create `.planning/phases/03-scratchpad-file-cleanup/03-02-SUMMARY.md`
</output>
