---
phase: 03-scratchpad-file-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ADMIN_DASHBOARD_SETUP.md
  - PERFORMANCE_AUDIT_REPORT.md
  - RESPONSIVE_SYSTEM.md
  - THEME_SYSTEM.md
  - LIGHTTOOL_GUIDE.md
  - build_out.txt
  - 'h origin main'
  - design.json
  - add_test_items.sql
  - update_notion_image_url.sql
  - database-migrations.sql
  - test-filter-changes.js
  - test-validation.js
  - LIGHTTOOL_IMPLEMENTATION/
  - performance-tasks/
  - .taskmaster/
  - aplikacja/
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-03
  - CLEAN-04
  - CLEAN-05
  - CLEAN-06

must_haves:
  truths:
    - 'All scratchpad markdown files are deleted from repository root'
    - 'All one-off SQL and JS test scripts are deleted from repository root'
    - 'LIGHTTOOL_IMPLEMENTATION, performance-tasks, and .taskmaster directories are fully removed'
    - 'aplikacja/ deletions are staged in git'
    - 'Whitelisted files (CLAUDE.md, AGENTS.md, ARCHITECTURE.md, Project.md, chatbot.json, components.json, tool configs) are untouched'
    - 'npm run build passes after all deletions'
  artifacts: []
  key_links: []
---

<objective>
Delete all high-confidence scratchpad files, obsolete scripts, junk artifacts, and obsolete directories from the repository. Stage the already-deleted aplikacja/ directory in git. Verify the build still passes.

Purpose: Remove 12+ root-level clutter files and 3 obsolete directories to leave only legitimate project files in the repository.
Output: Clean repository root with only active project files remaining.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scratchpad-file-cleanup/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete all high-confidence scratchpad files, scripts, junk artifacts, and obsolete directories</name>
  <files>
    ADMIN_DASHBOARD_SETUP.md
    PERFORMANCE_AUDIT_REPORT.md
    RESPONSIVE_SYSTEM.md
    THEME_SYSTEM.md
    LIGHTTOOL_GUIDE.md
    build_out.txt
    "h origin main"
    design.json
    add_test_items.sql
    update_notion_image_url.sql
    database-migrations.sql
    test-filter-changes.js
    test-validation.js
    LIGHTTOOL_IMPLEMENTATION/
    performance-tasks/
    .taskmaster/
    aplikacja/
  </files>
  <action>
Delete files in this exact order. Some files are git-tracked (from main), some are untracked (created on gsd-refactor). Use the appropriate command for each.

**Step 1: Delete untracked root-level files (use `rm`)**

These files appear as `??` in git status (untracked on gsd-refactor branch):

```bash
rm "ADMIN_DASHBOARD_SETUP.md"
rm "PERFORMANCE_AUDIT_REPORT.md"
rm "RESPONSIVE_SYSTEM.md"
rm "THEME_SYSTEM.md"
rm "LIGHTTOOL_GUIDE.md"
rm "build_out.txt"
rm "h origin main"
rm "design.json"
rm "add_test_items.sql"
rm "update_notion_image_url.sql"
rm "database-migrations.sql"
rm "test-filter-changes.js"
rm "test-validation.js"
```

**Step 2: Delete untracked directories (use `rm -rf`)**

These directories are untracked on gsd-refactor:

```bash
rm -rf LIGHTTOOL_IMPLEMENTATION/
rm -rf performance-tasks/
rm -rf .taskmaster/
```

**Step 3: Stage aplikacja/ deletions in git**

The aplikacja/ directory is already absent from the working tree (shows as `D` deleted in git status). Stage these deletions:

```bash
git add -u aplikacja/
```

This stages the already-performed deletions so they become part of the next commit.

**CRITICAL: Do NOT delete any of these whitelisted files:**

- chatbot.json (active Lottie animation import)
- components.json (shadcn/ui config)
- CLAUDE.md, AGENTS.md, ARCHITECTURE.md, Project.md
- .eslintrc.json, .prettierrc, .lintstagedrc.json, .gitignore, .gitattributes
- tsconfig.json, tailwind.config.js, postcss.config.js
- middleware.ts, package.json, package-lock.json
- Any hidden directories (.claude/, .planning/, .husky/, .git/)

**Verification after deletion:**
After all deletions, list remaining root-level files to confirm only legitimate files remain:

```bash
ls -la
```

  </action>
  <verify>
Confirm each file/directory is gone:
- `ls "ADMIN_DASHBOARD_SETUP.md"` should fail (file not found)
- `ls LIGHTTOOL_IMPLEMENTATION/` should fail (directory not found)
- `ls performance-tasks/` should fail (directory not found)
- `ls .taskmaster/` should fail (directory not found)
- `git status aplikacja/` should show staged deletions (green "deleted:")

Confirm whitelisted files are intact:

- `ls chatbot.json` should succeed
- `ls components.json` should succeed
- `ls CLAUDE.md` should succeed
  </verify>
  <done>
  All 13 root-level files deleted. All 3 obsolete directories deleted. aplikacja/ deletions staged in git. Zero whitelisted files affected. Root directory contains only legitimate project files.
  </done>
  </task>

<task type="auto">
  <name>Task 2: Build verification after deletions</name>
  <files>None (verification only)</files>
  <action>
Run the production build to confirm no deleted files were referenced by application code:

```bash
npm run build
```

The research confirmed that none of the deleted files are imported by any source code. chatbot.json (kept) is the only root-level JSON imported by code. design.json had zero code references and is safe to delete.

If the build fails with "Module not found" errors referencing any deleted file, restore that specific file and remove it from the deletion list. This is not expected based on research.

Also run lint to confirm no issues:

```bash
npm run lint
```

  </action>
  <verify>
- `npm run build` exits with code 0
- `npm run lint` exits with code 0 (or only pre-existing warnings)
- No "Module not found" errors referencing deleted files
  </verify>
  <done>
Build and lint pass cleanly after all file deletions, confirming no code dependencies were broken.
  </done>
</task>

</tasks>

<verification>
1. Root directory listing shows no scratchpad files, no one-off scripts, no junk artifacts
2. LIGHTTOOL_IMPLEMENTATION/, performance-tasks/, .taskmaster/ directories are gone
3. aplikacja/ deletions are staged in git
4. chatbot.json, components.json, and all whitelisted files are present
5. `npm run build` passes
6. `npm run lint` passes
</verification>

<success_criteria>

- 13 root-level files deleted (7 scratchpad docs + 5 one-off scripts + design.json)
- 3 directories fully removed (LIGHTTOOL_IMPLEMENTATION, performance-tasks, .taskmaster)
- aplikacja/ deletions staged in git
- Build passes with zero errors
- All whitelisted files preserved
  </success_criteria>

<output>
After completion, create `.planning/phases/03-scratchpad-file-cleanup/03-01-SUMMARY.md`
</output>
