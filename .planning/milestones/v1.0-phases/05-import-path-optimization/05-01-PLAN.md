---
phase: 05-import-path-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/articles/ArticleSubmissionDialog.tsx
  - src/components/chat/FloatingChat.tsx
  - src/components/editor/SaveStatusIndicator.tsx
  - src/lib/affiliate/affiliate-integration.ts
  - src/lib/services/ranking-service.ts
  - src/lib/services/search-service.ts
  - src/lib/services/vote-service.ts
  - chatbot.json
  - src/data/chatbot.json
  - .eslintrc.json
autonomous: true
requirements:
  - STRUCT-05
  - STRUCT-06

must_haves:
  truths:
    - 'All cross-directory imports use @/ alias (zero ../ imports remain)'
    - 'chatbot.json is accessible from FloatingChat via @/data/chatbot.json'
    - 'ESLint warns on future ../ imports to prevent regression'
    - 'Build passes with all import path changes'
  artifacts:
    - path: 'src/data/chatbot.json'
      provides: 'Chatbot config relocated under src/ for @/ alias access'
    - path: '.eslintrc.json'
      provides: 'no-restricted-imports rule preventing ../ imports'
  key_links:
    - from: 'src/components/chat/FloatingChat.tsx'
      to: 'src/data/chatbot.json'
      via: 'import from @/data/chatbot.json'
      pattern: "@/data/chatbot\\.json"
    - from: 'src/lib/services/vote-service.ts'
      to: 'src/lib/supabase.ts'
      via: 'import from @/lib/supabase'
      pattern: '@/lib/supabase'
---

<objective>
Convert all 8 cross-directory relative imports (`../`) to `@/` aliases, relocate `chatbot.json` into `src/data/` for alias accessibility, and add ESLint rule to prevent future `../` imports.

Purpose: Eliminate brittle relative imports that break on folder restructuring, completing the import standardization started in Phase 4.
Output: Zero `../` imports in codebase, chatbot.json under src/, ESLint guard rule.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-import-path-optimization/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert all 8 relative imports to @/ aliases and relocate chatbot.json</name>
  <files>
    src/components/articles/ArticleSubmissionDialog.tsx
    src/components/chat/FloatingChat.tsx
    src/components/editor/SaveStatusIndicator.tsx
    src/lib/affiliate/affiliate-integration.ts
    src/lib/services/ranking-service.ts
    src/lib/services/search-service.ts
    src/lib/services/vote-service.ts
    chatbot.json
    src/data/chatbot.json
  </files>
  <action>
    Manual import updates (user locked: no batch scripts).

    1. **Move chatbot.json**: Move `chatbot.json` from project root to `src/data/chatbot.json`. Create `src/data/` directory if it does not exist.

    2. **Update 8 imports** — change each file's relative import to its @/ equivalent:

       | File | Old Import | New Import |
       |------|-----------|------------|
       | `src/components/articles/ArticleSubmissionDialog.tsx:5` | `'../editor/CoverImageUpload'` | `'@/components/editor/CoverImageUpload'` |
       | `src/components/articles/ArticleSubmissionDialog.tsx:6` | `'../forms/inputs/TagInput'` | `'@/components/forms/inputs/TagInput'` |
       | `src/components/chat/FloatingChat.tsx:16` | `'../../../chatbot.json'` | `'@/data/chatbot.json'` |
       | `src/components/editor/SaveStatusIndicator.tsx:4` | `'../../hooks/useAutosave'` | `'@/hooks/useAutosave'` |
       | `src/lib/affiliate/affiliate-integration.ts:13` | `'../types/affiliate-library'` | `'@/lib/types/affiliate-library'` |
       | `src/lib/services/ranking-service.ts:9` | `'../supabase'` | `'@/lib/supabase'` |
       | `src/lib/services/search-service.ts:9` | `'../supabase'` | `'@/lib/supabase'` |
       | `src/lib/services/vote-service.ts:1` | `'../supabase'` | `'@/lib/supabase'` |

    3. **Delete** the old `chatbot.json` from project root after confirming the copy exists in `src/data/`.

    4. **Verify** no `../` imports remain: `grep -r "from '\.\.\/" src/ --include="*.ts" --include="*.tsx"` should return zero results.

  </action>
  <verify>
    Run: `grep -rn "from '\.\.\/" src/ --include="*.ts" --include="*.tsx"` — must return zero results.
    Run: `npm run build` — must pass with zero errors.
    Confirm `src/data/chatbot.json` exists and `chatbot.json` at root is removed.
  </verify>
  <done>Zero ../ imports in codebase. chatbot.json relocated to src/data/. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add ESLint no-restricted-imports rule for ../ prevention</name>
  <files>.eslintrc.json</files>
  <action>
    Add `no-restricted-imports` rule to `.eslintrc.json` to prevent future `../` imports from being introduced.

    Add this to the `"rules"` object:

    ```json
    "no-restricted-imports": ["warn", {
      "patterns": [{
        "group": ["../*"],
        "message": "Use @/ alias for cross-directory imports. Sibling ./ imports are allowed."
      }]
    }]
    ```

    Use `"warn"` severity (not `"error"`) so it doesn't block the build but flags violations during development.

    After adding the rule, run `npm run lint` to confirm no existing violations are flagged (there should be zero since Task 1 already converted all `../` imports).

  </action>
  <verify>
    Run: `npm run lint` — must pass with zero warnings related to no-restricted-imports (since all ../ imports were already fixed in Task 1).
    Confirm the rule exists in `.eslintrc.json`.
  </verify>
  <done>ESLint rule guards against future ../ imports. Lint passes cleanly.</done>
</task>

</tasks>

<verification>
1. `grep -rn "from '\.\.\/" src/ --include="*.ts" --include="*.tsx"` returns zero results
2. `npm run build` passes with zero errors
3. `npm run lint` passes
4. `src/data/chatbot.json` exists, root `chatbot.json` is gone
5. `.eslintrc.json` contains `no-restricted-imports` rule
</verification>

<success_criteria>

- Zero `../` relative imports in the entire src/ directory
- chatbot.json relocated from project root to src/data/chatbot.json
- ESLint rule prevents future `../` import regressions
- Build and lint both pass cleanly
  </success_criteria>

<output>
After completion, create `.planning/phases/05-import-path-optimization/05-01-SUMMARY.md`
</output>
