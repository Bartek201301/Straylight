---
phase: 04-folder-restructure-core
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/seo/metadata.ts
  - src/lib/seo/sitemap-utils.ts
  - src/lib/seo/structured-data.ts
  - src/lib/seo/opengraph.ts
  - src/lib/seo/twitter-cards.ts
  - src/lib/affiliate/affiliate.ts
  - src/lib/affiliate/affiliate-integration.ts
  - src/lib/mail/mailchimp.ts
  - src/lib/mail/mailchimp-errors.ts
  - src/lib/content/markdown.ts
  - src/lib/content/html-to-markdown.ts
  - src/lib/content/reading-time.ts
  - src/lib/content/article-status.ts
autonomous: true
requirements:
  - STRUCT-04
  - STRUCT-03

must_haves:
  truths:
    - 'Lib folder root files are organized into logical subdirectories by domain'
    - 'All imports referencing moved lib files are updated to new paths'
    - 'Existing lib subdirectories (auth, api, cache, constants, errors, middleware, quiz, services, types, validation, analytics) remain untouched'
    - 'Hooks directory stays at src/hooks/ (no move)'
    - 'Contexts directory stays at src/contexts/ (no move)'
  artifacts:
    - path: 'src/lib/seo/metadata.ts'
      provides: 'SEO metadata utilities grouped together'
    - path: 'src/lib/content/markdown.ts'
      provides: 'Content processing utilities grouped together'
    - path: 'src/lib/affiliate/affiliate.ts'
      provides: 'Affiliate utilities grouped together'
    - path: 'src/lib/mail/mailchimp.ts'
      provides: 'Mail utilities grouped together'
  key_links:
    - from: 'src/app/**/page.tsx'
      to: 'src/lib/seo/'
      via: '@/ imports updated to new seo/ subdir'
      pattern: '@/lib/seo/'
    - from: 'src/app/**/page.tsx'
      to: 'src/lib/content/'
      via: '@/ imports updated to new content/ subdir'
      pattern: '@/lib/content/'
    - from: 'src/components/**/*.tsx'
      to: 'src/lib/affiliate/'
      via: '@/ imports updated to new affiliate/ subdir'
      pattern: '@/lib/affiliate/'
---

<objective>
Reorganize the 13 flat files at src/lib/ root into 4 logical subdirectories: seo/, affiliate/, mail/, and content/. Update all import paths across the codebase.

Purpose: The lib/ root currently has 22+ files at the top level. Grouping related files into subdirectories makes the codebase navigable and establishes clear domain boundaries. Foundational files (supabase.ts, utils.ts, session-manager.ts, etc.) stay at root.

Output: 13 files organized into 4 new subdirectories with all imports updated. Build passes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-folder-restructure-core/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Group lib/ flat files into seo/, affiliate/, mail/, content/ subdirectories</name>
  <files>
    src/lib/seo/metadata.ts
    src/lib/seo/sitemap-utils.ts
    src/lib/seo/structured-data.ts
    src/lib/seo/opengraph.ts
    src/lib/seo/twitter-cards.ts
    src/lib/affiliate/affiliate.ts
    src/lib/affiliate/affiliate-integration.ts
    src/lib/mail/mailchimp.ts
    src/lib/mail/mailchimp-errors.ts
    src/lib/content/markdown.ts
    src/lib/content/html-to-markdown.ts
    src/lib/content/reading-time.ts
    src/lib/content/article-status.ts
  </files>
  <action>
Create 4 new subdirectories and move related files into them. Use `git mv` for all moves. After each group, update ALL imports across the entire codebase.

**Step 1: SEO group** (5 files):

```bash
mkdir -p src/lib/seo
git mv src/lib/metadata.ts src/lib/seo/metadata.ts
git mv src/lib/sitemap-utils.ts src/lib/seo/sitemap-utils.ts
git mv src/lib/structured-data.ts src/lib/seo/structured-data.ts
git mv src/lib/opengraph.ts src/lib/seo/opengraph.ts
git mv src/lib/twitter-cards.ts src/lib/seo/twitter-cards.ts
```

Update imports:

- Grep for `@/lib/metadata` (NOT `@/lib/metadata.ts` — TypeScript omits extensions) and replace with `@/lib/seo/metadata`
- Grep for `@/lib/sitemap-utils` and replace with `@/lib/seo/sitemap-utils`
- Grep for `@/lib/structured-data` and replace with `@/lib/seo/structured-data`
- Grep for `@/lib/opengraph` and replace with `@/lib/seo/opengraph`
- Grep for `@/lib/twitter-cards` and replace with `@/lib/seo/twitter-cards`

IMPORTANT: Be careful not to match `@/lib/seo-documentation` when replacing `@/lib/seo`. Use exact path matching.
Also check for any relative imports (../metadata, ./metadata) in other lib files that might reference these.

NOTE: There is an existing `src/lib/seo-documentation.md` file — this is documentation, not code. Leave it where it is (or note it for future cleanup — not in this plan's scope).

**Step 2: Affiliate group** (2 files):

```bash
mkdir -p src/lib/affiliate
git mv src/lib/affiliate.ts src/lib/affiliate/affiliate.ts
git mv src/lib/affiliate-integration.ts src/lib/affiliate/affiliate-integration.ts
```

Update imports:

- Grep for `@/lib/affiliate-integration` and replace with `@/lib/affiliate/affiliate-integration`
- Grep for `@/lib/affiliate` (but NOT `@/lib/affiliate-integration` or `@/lib/affiliate/`) and replace with `@/lib/affiliate/affiliate`

Be extremely careful with the `@/lib/affiliate` pattern — it must match the exact import `@/lib/affiliate` but not `@/lib/affiliate-integration` or `@/lib/affiliate/affiliate`. Use regex or check each file manually. The safest approach: first update `@/lib/affiliate-integration` (longer path), then update remaining `@/lib/affiliate` imports.

Also check if affiliate.ts and affiliate-integration.ts import from each other. If so, update those relative/absolute imports too.

**Step 3: Mail group** (2 files):

```bash
mkdir -p src/lib/mail
git mv src/lib/mailchimp.ts src/lib/mail/mailchimp.ts
git mv src/lib/mailchimp-errors.ts src/lib/mail/mailchimp-errors.ts
```

Update imports:

- Grep for `@/lib/mailchimp-errors` and replace with `@/lib/mail/mailchimp-errors`
- Grep for `@/lib/mailchimp` (but NOT `@/lib/mailchimp-errors` or `@/lib/mail/`) and replace with `@/lib/mail/mailchimp`

Same caution: update longer path first, then shorter.

Check if mailchimp.ts imports from mailchimp-errors.ts — if so, update the import (likely `./mailchimp-errors` which stays the same since both files move together).

**Step 4: Content group** (4 files):

```bash
mkdir -p src/lib/content
git mv src/lib/markdown.ts src/lib/content/markdown.ts
git mv src/lib/html-to-markdown.ts src/lib/content/html-to-markdown.ts
git mv src/lib/reading-time.ts src/lib/content/reading-time.ts
git mv src/lib/article-status.ts src/lib/content/article-status.ts
```

Update imports:

- Grep for `@/lib/markdown` and replace with `@/lib/content/markdown`
- Grep for `@/lib/html-to-markdown` and replace with `@/lib/content/html-to-markdown`
- Grep for `@/lib/reading-time` and replace with `@/lib/content/reading-time`
- Grep for `@/lib/article-status` and replace with `@/lib/content/article-status`

Check for cross-imports between these files (e.g., markdown.ts importing from reading-time.ts). Since they all move to the same directory, relative imports between them (`./reading-time`) will still work.

**Files that stay at lib/ root** (confirmed in research):

- supabase.ts, utils.ts, session-manager.ts, font-performance.ts, optimized-imports.ts, performance-monitor.ts, resourcePreloader.ts, analytics.ts

**Directories that stay untouched**:

- auth/, api/, cache/, constants/, errors/, middleware/, quiz/, services/, types/, validation/, analytics/

**Hooks and Contexts** — research recommends keeping at current locations:

- src/hooks/ stays as-is (STRUCT-04 satisfied — hooks are already properly organized)
- src/contexts/ stays as-is (app-wide contexts, standard Next.js pattern)

Run `npm run build` after all 4 groups are moved and imports updated.
</action>
<verify>
`npm run build` passes with zero TypeScript errors.
`npm run lint` produces no new errors.
Grep for old import paths returns zero results:

- `grep -r "@/lib/metadata[^/]" src/` → 0 results (excluding @/lib/metadata.ts which shouldn't exist)
- `grep -r "from '@/lib/sitemap-utils'" src/` → 0 results
- `grep -r "from '@/lib/structured-data'" src/` → 0 results
- `grep -r "from '@/lib/opengraph'" src/` → 0 results
- `grep -r "from '@/lib/twitter-cards'" src/` → 0 results
- `grep -r "from '@/lib/affiliate'" src/` → 0 results (only @/lib/affiliate/ should exist)
- `grep -r "from '@/lib/mailchimp'" src/` → 0 results (only @/lib/mail/ should exist)
- `grep -r "from '@/lib/markdown'" src/` → 0 results (only @/lib/content/ should exist)
- Same for html-to-markdown, reading-time, article-status

New subdirectories exist with correct files:

- src/lib/seo/ (5 files)
- src/lib/affiliate/ (2 files)
- src/lib/mail/ (2 files)
- src/lib/content/ (4 files)
  </verify>
  <done>
  13 flat lib/ files organized into 4 logical subdirectories. All imports updated. Existing subdirectories untouched. Hooks at src/hooks/ and contexts at src/contexts/ remain at current locations. Build passes.
  </done>
  </task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `npm run lint` produces no new warnings/errors
3. 4 new lib subdirectories created with correct files
4. No old import paths remain in codebase
5. Foundational files remain at lib/ root
6. Existing lib subdirectories untouched
7. src/hooks/ and src/contexts/ unchanged
</verification>

<success_criteria>
Lib folder organized with clear domain separation. 13 files moved into seo/, affiliate/, mail/, content/ subdirectories. All imports updated. Build passes. Hooks and contexts remain at established locations.
</success_criteria>

<output>
After completion, create `.planning/phases/04-folder-restructure-core/04-03-SUMMARY.md`
</output>
