---
phase: 09-final-validation
plan: 02
type: execute
wave: 2
depends_on: ['09-01']
files_modified: ['.planning/phases/09-final-validation/09-VALIDATION-REPORT.md']
autonomous: true
requirements: [VAL-05, VAL-06]

must_haves:
  truths:
    - 'All public page routes return HTTP 200'
    - 'All auth-required routes return HTTP 307 redirect for unauthenticated users'
    - 'No page route returns HTTP 500'
    - 'RLS is enabled on all public tables'
    - 'Before/after summary report documents all improvements'
  artifacts:
    - path: '.planning/phases/09-final-validation/09-VALIDATION-REPORT.md'
      provides: 'Comprehensive before/after summary with route verification and auth validation results'
  key_links:
    - from: 'middleware.ts'
      to: 'src/lib/auth/middleware-utils.ts'
      via: 'route protection definitions'
      pattern: 'authRequired|roleRequired'
---

<objective>
Verify all routes render correctly against a production build, validate auth/RLS enforcement, and produce the final before/after summary report.

Purpose: Confirm zero functional regression for route rendering and authentication — the two areas most at risk from 8 phases of restructuring. The summary report is the deliverable that documents the entire refactor's impact.

Output: Route verification results, auth/RLS validation results, comprehensive before/after summary report.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/metrics/baseline.json
@.planning/phases/09-final-validation/09-01-SUMMARY.md
@middleware.ts
@src/lib/auth/middleware-utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify all page routes render correctly against production build</name>
  <files></files>
  <action>
**Step 1: Build and start production server**

```bash
rm -rf .next/
npm run build
npm start &
# Wait for server to be ready
sleep 5
```

**Step 2: Create one-time route crawl script**

Create a temporary script (delete after use — per user decision, not kept in repo) that checks all 38 page routes. The script must:

- Check all public routes for HTTP 200
- Check auth-required routes — expect HTTP 307 (redirect to login) since crawl is unauthenticated
- Check admin routes — expect HTTP 307 (redirect)
- Treat HTTP 200 and 307/302 as OK (per research pitfall 3)
- Only flag HTTP 500 or connection errors as failures
- Handle dynamic routes like `/articles/[slug]` and `/profile/[handle]`: query the database for a published article slug and a user handle to test with real data. If no data available, accept that the route handler exists and 404 for invalid slugs is expected behavior (per research pitfall 7).

Route categories from middleware analysis:

**PUBLIC (expect 200):**
`/`, `/about`, `/articles`, `/library`, `/privacy`, `/terms`, `/cookies`, `/quiz`, `/quiz/questions`, `/quiz/results`, `/suggest-resource`, `/auth/signin`, `/auth/signup`, `/auth/forgot-password`, `/auth/reset-password`, `/auth/verify-email`, `/access-denied`

**AUTH-REQUIRED (expect 307 without auth):**
`/home`, `/dashboard`, `/dashboard/articles`, `/write`, `/preview`, `/profile`

**ADMIN (expect 307 without auth):**
`/admin`, `/admin/dashboard`, `/admin/articles`, `/admin/articles/pending`, `/admin/notifications`, `/admin/library/add`, `/admin/featured/articles`, `/admin/featured/tools`, `/admin/seo-tools`, `/admin/seo-tools/sitemap`, `/admin/seo-tools/schema`, `/admin/seo-tools/opengraph`

**Step 3: Run the crawl**

Execute the script against `http://localhost:3000`. Report results as a table.

**Step 4: Verify API route files exist on disk**

For the 62 API routes, do NOT crawl them (they require specific HTTP methods, auth headers, request bodies). Instead, verify the route.ts files exist on disk using glob. This confirms no routes were lost during restructuring.

**Step 5: Clean up**

Kill the production server. Delete the temporary crawl script.

**Step 6: Handle failures**

If any page route returns HTTP 500: investigate the error, fix the root cause, rebuild, and re-test. Critical fixes happen inline per user decision.
</action>
<verify>

- All public routes return HTTP 200
- All protected routes return HTTP 307 (auth redirect)
- No route returns HTTP 500
- All 62 API route.ts files exist on disk
  </verify>
  <done>All page routes verified against production build. Public routes return 200, protected routes properly redirect. No 500 errors. API route files confirmed present on disk.</done>
  </task>

<task type="auto">
  <name>Task 2: Validate auth/RLS enforcement and produce final summary report</name>
  <files>.planning/phases/09-final-validation/09-VALIDATION-REPORT.md</files>
  <action>
**Part A: Auth/RLS Validation (VAL-06)**

**A1: RLS verification via Supabase SQL (direct SQL approach per discretion recommendation)**

Use the Supabase MCP `execute_sql` tool to verify:

```sql
-- Verify RLS is enabled on all public tables
SELECT tablename, rowsecurity
FROM pg_tables
WHERE schemaname = 'public';
```

All tables must show `rowsecurity = true`. Flag any table with RLS disabled.

**A2: Verify middleware route protection configuration**

Read `middleware.ts` and `src/lib/auth/middleware-utils.ts`. Confirm:

- Auth-required routes are defined in the configuration
- Role-required routes specify correct roles (admin for /admin/\*, etc.)
- The middleware function properly checks session and role before allowing access

This is a code review check — the route crawl (Task 1) already verified the middleware is enforcing redirects for unauthenticated access.

**A3: Verify ProtectedRoute component exists and is used**

Confirm `src/components/auth/ProtectedRoute.tsx` (or similar) exists and is imported by protected pages. Grep for usage across the codebase.

**Note on test accounts:** Skip CVE-2025-29927 middleware bypass test per user locked decision (middleware logic was not changed). Test accounts exist but browser-based login testing is manual — the RLS SQL check and middleware code review provide equivalent confidence for automated validation.

**Part B: Final Summary Report**

Create `.planning/phases/09-final-validation/09-VALIDATION-REPORT.md` containing:

**1. Executive Summary**

- Refactor objective (zero functional regression, reduce complexity)
- Overall result (PASS/FAIL)
- Key metric: file/LOC reduction (THE headline number per user decision)

**2. Before/After Metrics Comparison**

- Read `.planning/metrics/baseline.json` and `.planning/metrics/post-refactor.json` (from plan 09-01)
- Full table with all 12 metrics, deltas, and percentage changes
- Highlight file count reduction and LOC reduction prominently
- Bundle size comparison with justification if increased

**3. Quality Gates**
| Gate | Result | Details |
|------|--------|---------|
| Build (VAL-01) | PASS/FAIL | ... |
| Lint (VAL-02) | PASS/FAIL | 387 → 0 warnings |
| Tests (VAL-03) | N/A | No test framework |
| TypeScript (VAL-04) | PASS/FAIL | ... |
| Routes (VAL-05) | PASS/FAIL | X/Y routes OK |
| Auth/RLS (VAL-06) | PASS/FAIL | ... |
| Bundle Size (VAL-07) | PASS/FAIL | ... |

**4. Route Verification Results**

- Summary of page route crawl results
- API route file existence check

**5. Auth & Security Validation**

- RLS status on all tables
- Middleware configuration review
- ProtectedRoute usage

**6. Phase-by-Phase Summary**

- Brief description of what each phase (1-8) accomplished
- Total execution time and plan count from STATE.md

**7. Regressions Found**

- List any regressions found and how they were fixed
- Or "None" if clean

This report is the final deliverable of the entire 9-phase refactor.
</action>
<verify>

- `.planning/phases/09-final-validation/09-VALIDATION-REPORT.md` exists
- Report contains before/after metrics table with all 12 metrics
- Report contains quality gate results for all 7 VAL requirements
- Report contains route verification results
- Report contains RLS verification results
- All SQL queries confirm RLS enabled on all public tables
  </verify>
  <done>Auth/RLS validated (all tables have RLS enabled, middleware properly configured). Final validation report created with comprehensive before/after metrics, quality gate results, route verification, and security validation. The report highlights file/LOC reduction as the headline metric.</done>
  </task>

</tasks>

<verification>
1. All public page routes return HTTP 200 from production build
2. All protected routes return HTTP 307 redirect for unauthenticated access
3. No route returns HTTP 500
4. All API route.ts files exist on disk
5. RLS is enabled on all public tables (verified via SQL)
6. Middleware route protection configuration is intact
7. Final validation report exists with complete before/after metrics
</verification>

<success_criteria>

- Zero route rendering failures (no 500s, no blank pages)
- Auth middleware properly redirects unauthenticated users
- RLS enabled on all database tables
- Comprehensive validation report documenting the entire refactor outcome
- File/LOC reduction prominently highlighted as headline metric
  </success_criteria>

<output>
After completion, create `.planning/phases/09-final-validation/09-02-SUMMARY.md`
</output>
