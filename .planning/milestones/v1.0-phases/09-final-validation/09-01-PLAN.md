---
phase: 09-final-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  [
    '.planning/metrics/post-refactor.json',
    '.planning/metrics/post-refactor.md',
    '.eslintrc.json',
  ]
autonomous: true
requirements: [VAL-01, VAL-02, VAL-03, VAL-04, VAL-07]

must_haves:
  truths:
    - 'npm run build completes with zero errors'
    - 'npm run lint reports zero warnings and zero errors'
    - 'npx tsc --noEmit reports zero TypeScript errors'
    - 'Post-refactor metrics JSON exists with all 12 metrics captured'
    - 'Bundle size is same or smaller than baseline 852kB shared JS'
  artifacts:
    - path: '.planning/metrics/post-refactor.json'
      provides: 'Post-refactor metrics snapshot for comparison'
    - path: '.planning/metrics/post-refactor.md'
      provides: 'Human-readable metrics report'
  key_links:
    - from: 'scripts/collect-metrics.js'
      to: '.planning/metrics/post-refactor.json'
      via: '--output-prefix post-refactor flag'
      pattern: 'output-prefix'
---

<objective>
Run all automated validation checks (build, lint, TypeScript, metrics) and fix all lint warnings to reach zero.

Purpose: Establish that the refactored codebase passes all automated quality gates and collect post-refactor metrics for comparison. The lint zero-warnings target is a user-locked decision — all 387 baseline warnings must be eliminated, not just new ones.

Output: Clean build, zero lint warnings/errors, zero TS errors, post-refactor metrics JSON and markdown files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/metrics/baseline.json
@scripts/collect-metrics.js
@.eslintrc.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated checks and fix all lint warnings to zero</name>
  <files>.eslintrc.json</files>
  <action>
Execute the following validation sequence:

**Step 1: Clear stale cache and run build**

```bash
rm -rf .next/
npm run build
```

If build fails, fix errors before proceeding. Build must exit 0 (VAL-01).

**Step 2: Run TypeScript check**

```bash
npx tsc --noEmit
```

Must report 0 errors (VAL-04). Baseline was 0. If errors exist, fix them.

**Step 3: Assess lint warnings**

```bash
npm run lint 2>&1
```

Count total warnings. User requires ZERO warnings (not baseline 387).

**Step 4: Fix all lint warnings to zero**

There are two main sources of warnings from research:

1. **`no-restricted-syntax` warnings for `'use client'`** (~80+ warnings from Phase 7 guard rule): These fire on files that legitimately need `'use client'`. Fix by expanding the ESLint overrides in `.eslintrc.json` to cover ALL files that legitimately use `'use client'`. Identify every file with `'use client'` at the top, then add override patterns covering those directories/files. Alternatively, if the rule was purely advisory guidance for the refactor, change it from `"warn"` to `"off"` since the refactor is now complete.

2. **`no-restricted-imports` warnings for `../*` imports**: Identify remaining relative cross-directory imports and convert them to `@/` alias paths.

3. **Any other ESLint warnings**: Fix per the specific rule. Use `npm run lint:fix` for auto-fixable issues first, then manually fix remaining.

After fixing, re-run `npm run lint` and confirm output shows zero warnings and zero errors.

**Step 5: Verify build still passes after lint fixes**

```bash
rm -rf .next/
npm run build
```

Confirm clean build after all lint fixes (no regressions introduced).

**VAL-03 note:** No test framework exists in this project (no test script in package.json). VAL-03 is vacuously satisfied. Document this in the summary.
</action>
<verify>
Run these commands and confirm all pass:

- `npm run build` exits 0
- `npm run lint` reports 0 warnings and 0 errors
- `npx tsc --noEmit` reports 0 errors
  </verify>
  <done>Build passes with zero errors, lint reports zero warnings and zero errors, TypeScript reports zero errors. All automated quality gates pass.</done>
  </task>

<task type="auto">
  <name>Task 2: Collect post-refactor metrics and compare with baseline</name>
  <files>.planning/metrics/post-refactor.json, .planning/metrics/post-refactor.md</files>
  <action>
**Step 1: Run the metrics collection script**

The metrics script from Phase 1 clears `.next/` cache and runs build internally. Run it with the post-refactor prefix:

```bash
node scripts/collect-metrics.js --output-prefix post-refactor
```

This produces:

- `.planning/metrics/post-refactor.json` — machine-readable metrics
- `.planning/metrics/post-refactor.md` — human-readable report

**Step 2: Compare with baseline**

Read both `.planning/metrics/baseline.json` and `.planning/metrics/post-refactor.json`. Compare these key metrics:

| Metric            | Baseline  | Post-Refactor | Delta |
| ----------------- | --------- | ------------- | ----- |
| Source Files      | 403       | ?             | ?     |
| Lines of Code     | 107,256   | ?             | ?     |
| Routes            | 100       | ?             | ?     |
| TS Errors         | 0         | 0             | 0     |
| Lint Warnings     | 387       | 0             | -387  |
| Lint Errors       | 0         | 0             | 0     |
| Circular Deps     | 0         | ?             | ?     |
| Build Time        | 117,266ms | ?             | ?     |
| Prod Dependencies | 36        | ?             | ?     |
| Dev Dependencies  | 21        | ?             | ?     |

**Step 3: Verify bundle size**

Check the build output for shared JS chunk size. Baseline from Phase 7: 852kB. Per user decision, small increase acceptable if file/LOC reduction is dramatic. Document the delta.

**Step 4: Record the comparison**

Note the file/LOC reduction prominently — this is the headline metric per user decision. Calculate percentage reductions.
</action>
<verify>

- `.planning/metrics/post-refactor.json` exists and contains all 12 metrics
- `.planning/metrics/post-refactor.md` exists
- Source file count is less than baseline 403
- Total LOC is less than baseline 107,256
- TypeScript errors remain 0
- Lint warnings are 0 (down from 387)
  </verify>
  <done>Post-refactor metrics collected, baseline comparison complete. File count and LOC show reduction from the refactor. Bundle size within acceptable tolerance. All metrics documented.</done>
  </task>

</tasks>

<verification>
1. `npm run build` exits with code 0 — no build errors
2. `npm run lint` output shows zero warnings and zero errors
3. `npx tsc --noEmit` output shows zero errors
4. `.planning/metrics/post-refactor.json` exists with valid data
5. Post-refactor source file count < 403 (baseline)
6. Post-refactor LOC < 107,256 (baseline)
7. Lint warnings dropped from 387 to 0
</verification>

<success_criteria>

- All automated quality gates pass (build, lint, tsc)
- Zero lint warnings achieved (user-locked clean slate requirement)
- Post-refactor metrics captured and compared with Phase 1 baseline
- File/LOC reduction documented as headline metric
- Bundle size within acceptable tolerance
  </success_criteria>

<output>
After completion, create `.planning/phases/09-final-validation/09-01-SUMMARY.md`
</output>
