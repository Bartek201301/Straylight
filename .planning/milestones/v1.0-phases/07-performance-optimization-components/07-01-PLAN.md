---
phase: 07-performance-optimization-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .eslintrc.json
autonomous: true
requirements:
  - PERF-01
  - PERF-03
must_haves:
  truths:
    - "ESLint warns when 'use client' appears in any file outside contexts/hooks/providers/effects/editor/forms/auth"
    - 'Baseline build output with per-route first-load JS sizes is captured before any optimizations'
  artifacts:
    - path: '.eslintrc.json'
      provides: 'no-restricted-syntax guard rule for use client pollution'
      contains: "ExpressionStatement[expression.value='use client']"
  key_links:
    - from: '.eslintrc.json'
      to: 'all src/ files'
      via: 'ESLint no-restricted-syntax rule'
      pattern: 'no-restricted-syntax'
---

<objective>
Capture baseline bundle metrics and add ESLint guard rule to prevent future `'use client'` pollution.

Purpose: Establish before-metrics for comparison and implement the locked decision (ESLint guard rule) before any optimization work begins.
Output: Build log with per-route first-load JS, updated .eslintrc.json with no-restricted-syntax rule.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-performance-optimization-components/07-RESEARCH.md
@.eslintrc.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture baseline build metrics and add ESLint use-client guard rule</name>
  <files>.eslintrc.json</files>
  <action>
**Step 1: Capture baseline metrics.**

Run `npm run build 2>&1 | tee .planning/phases/07-performance-optimization-components/build-before.log` to capture the full build output including per-route first-load JS sizes. If `tee` doesn't work on Windows, redirect to file and also display: `npm run build > .planning/phases/07-performance-optimization-components/build-before.log 2>&1` then read the file.

Extract and note the key routes' first-load JS:

- `/` (marketing homepage)
- `/articles` and `/articles/[slug]`
- `/home` (dashboard)
- `/write` (editor)
- `/admin/dashboard`
- Total shared JS chunks

Also run `npm run metrics -- --output-prefix pre-p07` if the metrics script exists and works.

**Step 2: Add ESLint `no-restricted-syntax` guard rule.**

Edit `.eslintrc.json` to add the `no-restricted-syntax` rule at warn level. The rule targets `ExpressionStatement[expression.value='use client']` with a helpful message. Add overrides to suppress the warning in directories where `'use client'` is legitimately expected:

- `src/contexts/**`
- `src/hooks/**`
- `src/components/providers/**`
- `src/components/effects/**`
- `src/components/editor/**`
- `src/components/forms/**`
- `src/components/auth/**`

The rule must be MERGED with the existing rules object (do not remove existing rules). The overrides array is new (does not exist yet in the config).

**Step 3: Verify ESLint config is valid.**

Run `npm run lint -- --max-warnings 9999 2>&1 | head -20` to confirm ESLint still works (no config parse errors). The `no-restricted-syntax` warnings will appear for files with `'use client'` outside the override directories -- this is expected and desired.
</action>
<verify>

1. Build log file exists at `.planning/phases/07-performance-optimization-components/build-before.log` and contains route sizes
2. `.eslintrc.json` contains `no-restricted-syntax` rule with `ExpressionStatement[expression.value='use client']` selector
3. `.eslintrc.json` has `overrides` array exempting contexts, hooks, providers, effects, editor, forms, auth
4. `npm run lint` does not produce config parse errors (warnings are expected and desired)
5. `npm run build` still passes
   </verify>
   <done>
   Baseline per-route JS sizes are recorded in build-before.log. ESLint guard rule warns on 'use client' in non-exempt files. Build and lint both pass without errors.
   </done>
   </task>

</tasks>

<verification>
- Build log exists and contains first-load JS per route
- ESLint config is valid JSON with the new rule and overrides
- `npm run build` passes
- `npm run lint` runs without config errors
</verification>

<success_criteria>

- Baseline metrics captured for before/after comparison
- ESLint guard rule active (locked decision implemented)
- No regressions introduced
  </success_criteria>

<output>
After completion, create `.planning/phases/07-performance-optimization-components/07-01-SUMMARY.md`
</output>
