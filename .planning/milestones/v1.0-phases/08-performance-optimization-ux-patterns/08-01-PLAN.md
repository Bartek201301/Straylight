---
phase: 08-performance-optimization-ux-patterns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/global-error.tsx
  - src/app/(marketing)/loading.tsx
  - src/app/(marketing)/error.tsx
  - src/app/(auth)/loading.tsx
  - src/app/(auth)/error.tsx
  - src/app/(dashboard)/loading.tsx
  - src/app/(dashboard)/error.tsx
  - src/app/(admin)/admin/loading.tsx
  - src/app/(admin)/admin/error.tsx
autonomous: true
requirements:
  - PERF-05

must_haves:
  truths:
    - 'Every route group has a loading.tsx fallback skeleton during route transitions'
    - 'Every route group has an error.tsx boundary preventing white screens on uncaught errors'
    - 'Root layout errors are caught by global-error.tsx instead of crashing the app'
    - 'All loading states use skeleton screens with pulse animation, not spinners'
    - 'All error states use card-base and neutral-* colors matching the design system'
  artifacts:
    - path: 'src/app/global-error.tsx'
      provides: 'Root layout error boundary with own html/body tags'
      contains: 'global-error'
    - path: 'src/app/(marketing)/loading.tsx'
      provides: 'Marketing route group loading skeleton'
      contains: 'BaseSkeleton'
    - path: 'src/app/(marketing)/error.tsx'
      provides: 'Marketing route group error boundary'
      contains: 'use client'
    - path: 'src/app/(auth)/loading.tsx'
      provides: 'Auth route group loading skeleton'
      contains: 'BaseSkeleton'
    - path: 'src/app/(auth)/error.tsx'
      provides: 'Auth route group error boundary'
      contains: 'use client'
    - path: 'src/app/(dashboard)/loading.tsx'
      provides: 'Dashboard route group loading skeleton'
      contains: 'BaseSkeleton'
    - path: 'src/app/(dashboard)/error.tsx'
      provides: 'Dashboard route group error boundary'
      contains: 'use client'
    - path: 'src/app/(admin)/admin/loading.tsx'
      provides: 'Admin route group loading skeleton'
      contains: 'BaseSkeleton'
    - path: 'src/app/(admin)/admin/error.tsx'
      provides: 'Admin route group error boundary'
      contains: 'use client'
  key_links:
    - from: 'src/app/(marketing)/loading.tsx'
      to: '@/components/ui/skeletons/BaseSkeleton'
      via: 'import'
      pattern: 'import.*BaseSkeleton.*from.*skeletons/BaseSkeleton'
    - from: 'src/app/(marketing)/error.tsx'
      to: '@/components/layout/Container'
      via: 'import'
      pattern: 'import.*Container.*from.*layout/Container'
---

<objective>
Create global-error.tsx and route-group level loading.tsx + error.tsx files for all 4 route groups, establishing blanket coverage for loading and error states.

Purpose: Every route in the app will have at least group-level loading skeleton and error boundary coverage, preventing white screens and providing consistent UX during route transitions.
Output: 9 new files (1 global-error + 4 loading + 4 error)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-performance-optimization-ux-patterns/08-RESEARCH.md

@src/app/error.tsx
@src/components/ui/skeletons/BaseSkeleton.tsx
@src/components/layout/Container.tsx
@src/app/(marketing)/articles/loading.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create global-error.tsx and route-group error boundaries</name>
  <files>
    src/app/global-error.tsx
    src/app/(marketing)/error.tsx
    src/app/(auth)/error.tsx
    src/app/(dashboard)/error.tsx
    src/app/(admin)/admin/error.tsx
  </files>
  <action>
Create 5 error boundary files following the existing root error.tsx pattern (card-base, neutral colors, try-again + go-home buttons, dev-only error details).

**src/app/global-error.tsx:**

- MUST include 'use client' directive
- MUST include its own html and body tags (required for global-error -- it replaces root layout)
- Use `html lang="en" className="dark"` and `body className="bg-black text-white antialiased"`
- Center a card with "Something went wrong" heading, neutral-400 description text, reset button (neutral-700 bg), and go-home button
- Include dev-only error details section (process.env.NODE_ENV === 'development')
- Do NOT import Container (root layout is replaced, Container may not render correctly)

**Route-group error.tsx files (marketing, auth, dashboard, admin):**

- MUST include 'use client' directive (error.tsx must be client components)
- Import Container from '@/components/layout/Container'
- Clone the visual pattern of the existing root error.tsx:
  - card-base wrapper with p-12 text-center
  - Icon circle (red-500/10 bg with red-400 warning SVG icon) -- keep this from root, it's not a "warning accent" it's a standard error indicator
  - "Something went wrong" heading in text-2xl font-bold text-white
  - Description in text-neutral-400
  - Try again button: bg-neutral-700 hover:bg-neutral-600 (NOT ai-purple -- use neutral per design system constraint)
  - Go to homepage button: bg-neutral-800 hover:bg-neutral-700 text-neutral-300
  - Dev-only error details section
- Each error file should log to console with section identifier: e.g., `console.error('Marketing section error:', error)`
- Use min-h-[60vh] (not min-h-screen) for route-group errors since they render inside the root layout
  </action>
  <verify>
  Run `npx tsc --noEmit` to verify TypeScript compilation. Run `npm run build` to confirm all error boundaries compile correctly. Grep all new error.tsx files for 'use client' directive.
  </verify>
  <done>
  5 error boundary files exist: 1 global-error.tsx at app root + 4 route-group error.tsx files. All use 'use client', all follow card-base + neutral color design system, all have try-again and go-home buttons, all have dev-only error details.
  </done>
  </task>

<task type="auto">
  <name>Task 2: Create route-group level loading skeletons</name>
  <files>
    src/app/(marketing)/loading.tsx
    src/app/(auth)/loading.tsx
    src/app/(dashboard)/loading.tsx
    src/app/(admin)/admin/loading.tsx
  </files>
  <action>
Create 4 loading.tsx files using BaseSkeleton with variant="pulse" (NOT shimmer -- user locked "skeleton screens not shimmer effects"). Import from '@/components/ui/skeletons/BaseSkeleton'.

**src/app/(marketing)/loading.tsx:**

- Import Container and BaseSkeleton + TextLineSkeleton
- Render a generic content page skeleton: Container wrapper, py-12, max-w-4xl mx-auto
- Title skeleton: BaseSkeleton className="h-10 w-64" variant="pulse" rounded="lg"
- Subtitle: BaseSkeleton className="h-5 w-96" variant="pulse" rounded="md"
- 3-line text block: TextLineSkeleton lines={3} variant="pulse"
- Content area: BaseSkeleton className="h-64 w-full" variant="pulse" rounded="lg"
- Do NOT use 'use client' (loading.tsx should be server components when possible, but BaseSkeleton is 'use client' so this will still work as a server component importing a client component)

**src/app/(auth)/loading.tsx:**

- Auth-specific centered form skeleton (no Container -- auth pages are typically full-screen centered)
- min-h-screen flex items-center justify-center bg-black
- Card area: w-full max-w-md p-8 space-y-6
- Logo/title skeleton at top center
- card-base wrapper with p-6 space-y-4 containing:
  - 2 input field skeletons (h-10 w-full)
  - 1 button skeleton (h-12 w-full)

**src/app/(dashboard)/loading.tsx:**

- Import Container and BaseSkeleton
- Render a generic dashboard skeleton: Container, py-8
- Navigation/header skeleton: h-8 w-48 for page title
- Content area: 2-column grid on lg, single column on mobile
- Left column: 2 card skeletons (card-base p-6 with internal skeleton lines)
- Right column: 1 tall card skeleton

**src/app/(admin)/admin/loading.tsx:**

- Import DashboardSkeleton from '@/components/ui/skeletons/DashboardSkeleton' if it renders a good admin layout skeleton
- OR build a similar admin panel skeleton using BaseSkeleton: sidebar placeholder + main content area
- Check DashboardSkeleton first -- if it looks appropriate, use it directly
- Wrap in Container, py-8
  </action>
  <verify>
  Run `npm run build` to confirm all loading files compile and are recognized by Next.js. Check build output for route segments showing loading boundaries.
  </verify>
  <done>
  4 route-group loading.tsx files exist. All use skeleton screens with pulse variant. Marketing uses generic content skeleton, auth uses centered form skeleton, dashboard uses multi-panel skeleton, admin uses dashboard-appropriate skeleton. No spinners, no shimmer animation, no gray-\* colors.
  </done>
  </task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `npm run lint` passes (or only pre-existing warnings)
3. All 9 new files exist in their correct locations
4. All error.tsx files contain 'use client' directive
5. All loading.tsx files use BaseSkeleton with variant="pulse"
6. No files import from src/components/ui/Skeleton.tsx (the gray-* one)
7. No data fetching logic modified in any existing files
8. global-error.tsx includes own html and body tags
</verification>

<success_criteria>
Every route in the application is covered by at least a group-level loading skeleton and error boundary. The root layout has global-error.tsx protection. All new files follow the design system (card-base, neutral-\*, pulse skeletons).
</success_criteria>

<output>
After completion, create `.planning/phases/08-performance-optimization-ux-patterns/08-01-SUMMARY.md`
</output>
