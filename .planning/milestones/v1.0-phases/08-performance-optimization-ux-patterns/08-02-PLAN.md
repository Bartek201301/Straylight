---
phase: 08-performance-optimization-ux-patterns
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/app/(marketing)/articles/[slug]/loading.tsx
  - src/app/(marketing)/library/loading.tsx
  - src/app/(marketing)/profile/[handle]/loading.tsx
  - src/app/(marketing)/quiz/loading.tsx
  - src/app/(dashboard)/dashboard/loading.tsx
  - src/app/(dashboard)/home/loading.tsx
  - src/app/(dashboard)/write/loading.tsx
  - src/app/(admin)/admin/dashboard/loading.tsx
autonomous: true
requirements:
  - PERF-05

must_haves:
  truths:
    - 'High-traffic routes show tailored skeleton screens matching their actual page layout'
    - 'Article detail page shows title/author/content skeleton during route transition'
    - 'Library page shows a grid of card skeletons matching the actual card grid'
    - 'Dashboard routes show appropriate multi-panel skeletons'
    - 'Editor/write page shows a skeleton matching the editor layout'
  artifacts:
    - path: 'src/app/(marketing)/articles/[slug]/loading.tsx'
      provides: 'Article detail page skeleton with title, author, content areas'
      contains: 'BaseSkeleton'
    - path: 'src/app/(marketing)/library/loading.tsx'
      provides: 'Library grid skeleton with card placeholders'
      contains: 'BaseSkeleton'
    - path: 'src/app/(marketing)/profile/[handle]/loading.tsx'
      provides: 'Profile page skeleton with avatar, bio, content areas'
      contains: 'CircleSkeleton'
    - path: 'src/app/(marketing)/quiz/loading.tsx'
      provides: 'Quiz page skeleton'
      contains: 'BaseSkeleton'
    - path: 'src/app/(dashboard)/dashboard/loading.tsx'
      provides: 'Dashboard tabbed interface skeleton'
      contains: 'BaseSkeleton'
    - path: 'src/app/(dashboard)/home/loading.tsx'
      provides: 'Home feed skeleton with article card placeholders'
      contains: 'BaseSkeleton'
    - path: 'src/app/(dashboard)/write/loading.tsx'
      provides: 'Editor page skeleton with toolbar and content area'
      contains: 'BaseSkeleton'
    - path: 'src/app/(admin)/admin/dashboard/loading.tsx'
      provides: 'Admin dashboard skeleton with stats and panels'
      contains: 'BaseSkeleton'
  key_links:
    - from: 'src/app/(marketing)/articles/[slug]/loading.tsx'
      to: '@/components/ui/skeletons/BaseSkeleton'
      via: 'import'
      pattern: 'import.*BaseSkeleton.*from.*skeletons/BaseSkeleton'
    - from: 'src/app/(marketing)/library/loading.tsx'
      to: '@/components/layout/Container'
      via: 'import'
      pattern: 'import.*Container.*from.*layout/Container'
---

<objective>
Create route-specific loading.tsx files for 8 high-traffic routes with tailored skeleton screens that match each page's actual layout structure.

Purpose: High-traffic routes get seamless loading-to-content transitions with skeletons that match the actual page layout, reducing perceived layout shift and improving UX.
Output: 8 new route-specific loading.tsx files
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-performance-optimization-ux-patterns/08-RESEARCH.md
@.planning/phases/08-performance-optimization-ux-patterns/08-01-SUMMARY.md

@src/components/ui/skeletons/BaseSkeleton.tsx
@src/components/articles/ArticleCardSkeleton.tsx
@src/components/ui/skeletons/DashboardSkeleton.tsx
@src/components/layout/Container.tsx
@src/app/(marketing)/articles/loading.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create marketing route-specific loading skeletons</name>
  <files>
    src/app/(marketing)/articles/[slug]/loading.tsx
    src/app/(marketing)/library/loading.tsx
    src/app/(marketing)/profile/[handle]/loading.tsx
    src/app/(marketing)/quiz/loading.tsx
  </files>
  <action>
Create 4 route-specific loading files for marketing sub-routes. All use variant="pulse". All import from '@/components/ui/skeletons/BaseSkeleton'. Check each route's actual page.tsx to understand the layout structure before building the skeleton.

**src/app/(marketing)/articles/[slug]/loading.tsx:**

- Read the actual article detail page to understand layout
- Container wrapper, py-12, max-w-4xl mx-auto
- Title: BaseSkeleton h-12 w-3/4 mb-4
- Author row: flex items-center space-x-3 mb-8 with CircleSkeleton size="md" + two small text skeletons (name + date)
- Content body: TextLineSkeleton lines={4}, then a large image placeholder (h-48 w-full), then TextLineSkeleton lines={6}
- Optional: sidebar TOC skeleton if the article page has a TOC

**src/app/(marketing)/library/loading.tsx:**

- Read the actual library page to understand the grid layout
- Container wrapper, py-12
- Title skeleton + subtitle skeleton at top
- Filter/tag bar skeleton: flex of 5-6 small rounded pill skeletons (h-8 w-20)
- Grid of card skeletons: responsive grid matching actual layout (likely 3-4 columns on desktop)
- Each card: card-base with image placeholder (h-40), title skeleton, description lines, tag pill

**src/app/(marketing)/profile/[handle]/loading.tsx:**

- Read the actual profile page to understand layout
- Container wrapper, py-12
- Profile header: flex with large CircleSkeleton (xl), plus name/bio skeletons
- Stats row: 3-4 small stat boxes
- Content section: article list or activity feed skeleton

**src/app/(marketing)/quiz/loading.tsx:**

- Container wrapper, py-12, max-w-2xl mx-auto (quizzes are typically narrower)
- Title + description skeletons
- Quiz card skeleton: card-base with question text lines + 4 answer option skeletons (h-12 each)
  </action>
  <verify>
  Run `npm run build` to confirm all 4 files compile. Verify no imports from Skeleton.tsx (gray colors). Check all use variant="pulse".
  </verify>
  <done>
  4 marketing route-specific loading.tsx files exist with tailored skeletons matching each page's layout. Article detail has title/author/content skeleton, library has grid cards, profile has avatar/bio/stats, quiz has question/answer skeleton.
  </done>
  </task>

<task type="auto">
  <name>Task 2: Create dashboard and admin route-specific loading skeletons</name>
  <files>
    src/app/(dashboard)/dashboard/loading.tsx
    src/app/(dashboard)/home/loading.tsx
    src/app/(dashboard)/write/loading.tsx
    src/app/(admin)/admin/dashboard/loading.tsx
  </files>
  <action>
Create 4 route-specific loading files for dashboard and admin routes. All use variant="pulse".

**src/app/(dashboard)/dashboard/loading.tsx:**

- Read actual dashboard page to understand tabs/layout
- Container wrapper, py-8
- Page title skeleton (h-8 w-48)
- Tab bar skeleton: flex of 4-5 tab skeletons (h-10 w-24 each)
- Tab content area: card-base with internal skeleton content matching the most common tab (likely overview with stats cards)

**src/app/(dashboard)/home/loading.tsx:**

- Read actual home page to understand feed layout
- Container wrapper, py-8
- Welcome/greeting skeleton at top
- Feed of article cards: use ArticleCardSkeleton if available, or build card skeletons
- 3-4 card skeletons in a vertical list or grid

**src/app/(dashboard)/write/loading.tsx:**

- Read actual write/editor page to understand layout
- Full-width layout (editor pages typically use more width)
- Toolbar skeleton: h-12 w-full with small button skeletons inside
- Title input skeleton: h-14 w-full
- Editor content area: large skeleton block (h-96 w-full) representing the editing area
- Sidebar skeleton if the editor has a sidebar (cover image upload, settings)

**src/app/(admin)/admin/dashboard/loading.tsx:**

- Check if DashboardSkeleton component is appropriate -- read DashboardSkeleton.tsx first
- If DashboardSkeleton works: import and render it with Container wrapper
- If not suitable: build admin dashboard skeleton with stat cards row (4 cards) + main content panels
- Use card-base for card containers
  </action>
  <verify>
  Run `npm run build` to confirm all 4 files compile. Run `npm run lint` to check for issues. Verify the complete set of 17 new files across both plans (1 global-error + 4 group error + 4 group loading + 8 route-specific loading).
  </verify>
  <done>
  4 dashboard/admin route-specific loading.tsx files exist. Dashboard shows tabbed interface skeleton, home shows feed skeleton, write shows editor skeleton, admin dashboard shows stats+panels skeleton. All use pulse variant, all follow design system.
  </done>
  </task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `npm run lint` passes (or only pre-existing warnings)
3. All 8 route-specific loading.tsx files exist in correct locations
4. All loading files use BaseSkeleton with variant="pulse"
5. No files import from src/components/ui/Skeleton.tsx
6. No data fetching logic modified in any existing files
7. Route-specific skeletons approximate the actual page layout structure
8. Combined with Plan 01: total 17 new files (1 global-error + 4 group error + 4 group loading + 8 route-specific loading)
</verification>

<success_criteria>
High-traffic routes show tailored skeleton screens during route transitions that match their actual page layouts. Combined with Plan 01, every route segment has loading and error coverage. The entire app has consistent loading (skeletons) and error (card-base boundaries) UX patterns.
</success_criteria>

<output>
After completion, create `.planning/phases/08-performance-optimization-ux-patterns/08-02-SUMMARY.md`
</output>
